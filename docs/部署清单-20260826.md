# 云吞吞 AI 文游 部署清单（累计，2026-08-26）

> 覆盖 uugame 融合轮（a9c5c36/57aabb3/1bb668c）+ UI 改版批 B/C/D 轮（待部署）+ 此前全部未执行项。
> 线上域名：worker = ai.blupure.cn（Cloudflare Workers），PB = db.blupure.cn，前端 = bitlife.blupure.cn（GitHub Pages）。

---

## 〇e、封面收官轮：自建卡 AI 封面 + 官方卡预生成 + UI 微调 + 性能优化（2026-08-27 已部署 ✅：worker 版本 a113e5e7→最新 + 提交 5720e61）

> **本轮范围**：① 自建卡封面（编辑器 AI 生成 100 云币/张 + 私有 KV 缓存 + 上传/移除）；② 官方卡封面 115 张**预生成**（KV 秒回，用户不再现场等出图）；③ UI 微调（立即游玩按钮靠右自适应 + 角色台词蓝色/深夜粉色）；④ 性能优化（cover 全局并发队列 + worker 503 重试）。

**worker.js（已 wrangler deploy）：**
- `/api/cover/generate` 支持 `consume:true`（自建卡付费）：**100 云币/张**，私有 KV key `u:{userId}:cv:{prompt}`（跨设备同用户复用，不共享）；余额不足 402 `INSUFFICIENT_COIN`；**生成成功后才扣费**（PB PATCH 原子写，命中缓存不扣）
- Agnes 出图 **503「text image queue is full」自动重试 3 次**（等 3s 间隔）——预生成 4 并发实测触发队列满，重试后成功
- **SILICONFLOW 兜底修复**：实测 SF 返回 `{images:[{url:s3...}]}` 而非 b64_json → 新增 url 分支下载转 base64（`response_format:"url"`）
- 502 错误透出 `detail.genErr`（Agnes/SF 原始错误，诊断用，无副作用）

**index.html（提交 5720e61，GitHub Pages 已推送）：**
- 编辑器「🖼️ 封面图」section：4:3 预览 + 上传（canvas 压缩 ≤640px）/ **AI 生成（100 云币）**（提示词 ≤120 字）/ 移除；官方卡 AI 生成拦截
- `CoverService.ensureSelf`：coverUrl → 私有缓存（**记录提示词 hash，改提示词重新生成重新扣费**）→ consume 请求；`opts.force` 支持 AI 生成跳过 coverUrl；paint 分流（自建卡 ensureSelf / 官方卡 ensure）
- **封面全局并发队列**：`enqueueCover` 峰值 2 in-flight（视口内多卡 paint 不再并发打爆生成接口）
- 首页卡片「立即游玩」按钮：flex space-between 靠右 + 宽度自适应（贴内容区右下角）
- 角色台词 `[对白]` 包 `.npc-dialogue`：亮色蓝色 `#2f6fd0` / 深夜粉色 `#f2a6c6`（markdown 渲染后正则替换，安全）
- 首页卡片封面懒加载（IntersectionObserver rootMargin 300px，已在上轮）

**官方卡封面预生成（2026-08-27 凌晨完成）：**
- `F:/temp/ui-check/pregen_cover.mjs`：直接读 `scenarios/category_*.json`（115 张）→ promptOf（与前端 CoverService.promptOf 同步副本）→ 并发 4 POST 生产 cover 接口 → KV 共享缓存
- **115/115 全部成功**（大部分 1-3s KV 命中秒回；未命中真实出图 20-155s）；预生成用测试账号 coverbatch@test.local（**已用后删除**）
- ⚠️ 未来新增官方卡需重跑 pregen_cover.mjs（否则首个用户打开会触发真实生成等待 20-155s）

**验证：**
1. cover_self_probe 14/14 ✅（consume 请求体/扣费/私有 KV/402/官方卡拦截/并发队列 peak=2）
2. ui_tweak_probe 7/7 ✅（按钮靠右自适应 + 台词染色双主题）
3. cover_prompt_probe 22/22 ✅、全量回归 45/45 ✅（pay_wechat/header 偶发导航超时为本机网络抖动，单跑全绿）
4. 生产 cover 实测：Agnes 2.1-flash 出图 200 ✅、SF Kolors 兜底 200 ✅、CogView-3-Flash 实测可用但**强制水印不用于封面**（watermark:false 无效）

---

## 〇d、uugame 融合批 E 收官轮（2026-08-26 已部署 ✅：worker 版本 bf41d9a0 + 提交 0ece369）

> **本轮范围**：大地图系统 + 封面提示词模板（Agnes 免费模型）+ NPC 双轴好感 + 世界书 + 结局条件 + 批 F（属性定义/固定事件/周期收入）。
> **部署动作**：① worker.js wrangler deploy（封面模型切 Agnes Image 2.1 Flash 免费）；② GitHub Pages 全量上传（index.html + `maps/` 6 张 webp + scenarios/ + icons/）。

**worker.js（已 wrangler deploy，版本 bf41d9a0）：**
- `/api/cover/generate` 封面模型改造：**Agnes Image 2.1 Flash（免费）优先**——`apihub.agnes-ai.com/v1/images/generations`，body `{model:"agnes-image-2.1-flash", prompt, size:"1K", ratio:"4:3", extra_body:{response_format:"url"}}`（1152x864 横版 4:3）
  - ⚠️ **实测结论（2026-08-26）**：`b64_json` 与 `return_base64:true` 路径服务端挂起（>240s 无响应，疑似 Agnes b64 端故障）；**URL 输出约 11s 正常** → worker 下载 URL 图片（30s 超时）→ `toBase64`（0x8000 分块防栈溢出）→ `data:image/png;base64,` dataURL 返回
  - **KV 持久缓存**：新 namespace `COVER_CACHE`（wrangler.toml，id 05ff1df6...c847）7 天 TTL——内存 3 分钟 → KV get → Agnes → 下载 → KV put，同图二次请求约 19s 命中
  - **SILICONFLOW 512 兜底保留**（Agnes 失败/未配置/空数据，30s 超时）
- 其余（支付/解锁/签到）本轮无改动

**index.html（GitHub Pages 全量上传）：**
- **🗺️ 大地图系统**：structured.map（bg + bgUrl + places[] x/y 比例坐标）；游玩页缩略图横幅 + 全屏 overlay 图钉选点（有 desc → 详情 modal「前往」，无 desc → 直达 moveToLocation 推进剧情）；玩家自定义 per-card override（bitlife_map_override_v1，6 图单选 + URL + 本地上传，设置页「主题与展示」区）；编辑器「🗺️ 地图与地点」section（6 图 radio + URL + 地点动态行 + 预览图拖点选位）
- **🖼️ 封面提示词模板**：CoverService.promptOf 重写 6 段结构（主体+环境+情绪+风格+构图+画质）+ 23 分类 CATEGORY_STYLE_MAP + 10 主题叠加，≤120 字；缓存改存 {image, promptHash}，hash 不符即绕过（旧 7 天缓存自动失效）；封面展示容器统一 `aspect-ratio:4/3 + object-fit:cover`（首页卡片/轮播/推荐榜）
- **💞 NPC 双轴好感**：state.npcs[].affection（-100~100）+ favor 友善度；AI 协议（开局/审计/聊天）双轴说明；渲染双条（⭐ 友善 + ♥ 爱慕，负值样式）；AI 只回 favor 不漂移 affection（轴级抑制）；编辑器 NPC 行「爱慕初值」
- **📖 世界书**：structured.worldbook（≤20 条，content ≤200 字）；WorldbookService 关键词命中（userText + 最近 AI 卡 + state.location）→ 主线 systemPrompt【世界书条目】段（最高优先级声明）+ NPC 聊天 userPrompt 注入；编辑器「📖 世界书」动态行
- **🏆 结局条件**：structured.endings（favor/affection/rounds/event）；游玩页结局按钮「✅可达成」徽章 + confirm 列出已满足条件；requestEnding systemPrompt 注入条件参考（全部满足 ≥A / ≥2 条 ≥B）；fallbackEnding 提级（allMet → S/A、≥2 → B、≥1 → C 保底）
- **批 F**：属性定义（编辑器属性表+上限+AI 限定）/ 固定事件（date+round 双触发）/ 周期收入（N 天自动+数值）

**GitHub Pages 必须上传：**
| 文件/目录 | 说明 |
|---|---|
| `index.html` | 本轮全部前端改动 |
| `maps/` 6 个 webp | school/palace/city/doomsday/magic/parchment（<250KB/张，19MB 原图压缩产物）|
| `scenarios/` 全部 46 个文件 | 同前轮 |
| `icons/` 全部 20 个 | 同前轮 |

**验证顺序：**
1. 全量回归 runner（45 探针）全绿 ✅（2026-08-26，封面改动后复跑 45/45）
2. worker.js `wrangler deploy` ✅（版本 54420fb3 + 89a82dbb + bf41d9a0，含 Agnes 90s/SF 30s 超时修复 + URL 输出 + KV 缓存）
3. GitHub Pages 上传 ✅（提交 0ece369，`maps/` 6 图 200 + index.html 200）
4. **cover 生产出图 ✅ 验证通过（2026-08-26）**：首次请求约 34s 完成（Agnes URL 11s + 下载 + 转 base64，dataURL 2.1MB），二次请求约 19s KV 命中，两次返回同一张图。前端 `CoverService.ensure` 对 ≥300KB 图跳过 localStorage（5MB 限制，KV 兜底）。**待办全部清零**
5. 生产抽查（后续）：编辑器存地图 → 游玩页缩略图 → 图钉选点推进

---

## 〇c、UI 改版批 B/C/D 轮（2026-08-26 已部署 ✅：worker 版本 b807182c + 提交 2cbe653）

> **部署完成记录**：PB unlock_price 字段已加（幂等脚本重跑通过）；worker.js 已 wrangler deploy（版本 b807182c，含半价解锁修复 + 封面接口 + 188/89/168 档位）；GitHub Pages 已上线（提交 2cbe653，skeletons.js + scenarios 46 文件已确认可访问）；生产 e2e 7/8（唯一失败为模型池随机输出 JSON 格式，非部署回归）；runner 全量 38/38。

**PB 变更（脚本 scripts/pb_add_unlock_price.mjs 幂等可重跑）：**
- community_cards 新增 `unlock_price` Number（0~500，默认 0=免费）

**worker.js：**
- 终身会员限时价 89 → **188**（LIFETIME_PLAN.price，下单/金额校验同源）
- 充值档位新增 **c89（¥89 = 89000+17800 币）/ c168（¥168 = 168000+33600 币）**（CHARGE_PLANS 与前端表同步）
- 新增 `/api/cover/generate`（POST，需登录）：SILICONFLOW FLUX.1-schnell 文生图 → 512px JPEG base64；同 prompt 3 分钟实例缓存防重复耗额度
- **半价解锁修复**：unlock 路由此前按 unlock_price 全价扣费，现与列表 halfOff 同一判定（本周付费卡中 play_count 最高）→ 实付 ceil(price/2)，作者分成按实付价 floor(实付/2)
- 已有（批 A 已改）：unlock 路由官方免费/社区按 unlock_price/分成 max(1,floor(price/2))；community 列表返回 unlock_price + halfOff（每周一 top1 付费卡限免半价）；CHECKIN_FIRST_BONUS=300

**index.html（GitHub Pages 全量上传）：**
- 首页卡片改版（B4）：2 列 grid、卡片封面区（≥96px 渐变+流光/呼吸动效）、按钮=详情+立即游玩（右下角，flex 均分统一宽度）、移除设置按钮/置顶组/分组折叠、全部按热度排序
- AI 封面按需生成（B1）：仅轮播 + 推荐榜 top3 展示位，CoverService 接 `/api/cover/generate` → localStorage `bitlife_card_cover_v1` 缓存 TTL 7 天，未生成用渐变+emoji 动效占位
- 卡片 tag（C5）：官方/自定义 + category + genre 拆分，≤4 个
- 我的页（C1/C2）：新增「最近游玩」（lastPlayedAt 倒序不限量，左滑移出仅移出列表）与「收藏」（左滑取消收藏）section
- 左滑只剩黄色收藏（C3）：官方卡走 hot 表 _collected、社区卡（已下载）走本地集合 bitlife_card_collects_v1；详情页收藏社区卡同步本地集合
- 「这段故事」（C4）：从设置页移到游玩页 play-advance-area（推进按钮旁）
- 上架定价（D2）：上传前弹窗设置 unlock_price 0~500（免费无分成/付费解锁作者得一半）；我的剧本收益按新分成公式显示
- 社区卡下载保留 unlock_price/halfOff 字段（半价卡显示「限免半价」）
- 终身会员限时价 188、充值面板新增 89/168 档
- 会员/设置页文案补充：BYOK 不耗云币但解锁付费社区卡仍需云币

**验证：** home_rework_probe 20/20（2列/无设置/左滑收藏/tag/热度序/最近游玩/收藏/这段故事位置/上架弹窗/89/168/188 档/轮播封面）；runner 全量；worker_api_probe（unlock 官方免费/付费/分成/半价断言）

---

## 〇、用户名登录轮（2026-08-26 完成，提交 b0a098e + worker 版本 ba2f7c6d）

**PB 变更（已执行，脚本 scripts/pb_add_username.mjs 幂等可重跑）：**
- users 集合新增 `username` 字段：text，min 3 / max 30，pattern `^[a-zA-Z0-9_-]+$`，非必填
- 唯一索引：`CREATE UNIQUE INDEX idx_users_username ON users (username) WHERE username != ''`（条件索引兼容存量空值用户）
- `passwordAuth.identityFields = ["email", "username"]`（⚠️ 关键：新版 PB 默认只认 email，不加此项用户名无法登录）

**worker.js：**
- 社交登录（QQ/微信）建号时生成规范 username：`s_{type}_{uid净化}`（如 s_qq_uidqq1，净化 uid 截断 24，pattern 合规）
- 存量社交用户（无 username）下次登录时自动补齐

**index.html：**
- 登录输入框改「邮箱或用户名」（type=text，去邮箱正则，PB 原生支持两种身份）
- 注册表单新增可选用户名输入框（3-30 位字母/数字/下划线，不填则 PB 自动生成）
- 社交登录后展示 username 优先于内部邮箱

**验证：** PB 直连 username/email 双登录 ✅、唯一索引拦截重复 ✅、social_login_probe 31/31、全量 runner 36/36、生产 e2e 8/8、production_verify 13/13

## 〇b、邮箱验证码注册轮（2026-08-26 完成，提交 024b3d8 + worker 版本 86fb6aad）

**PB 变更（已执行，脚本 scripts/pb_add_email_codes.mjs 幂等可重跑）：**
- 新建 `email_codes` 集合：email(text,必填) / code(text,6-10,必填) / expires_at(text,必填) / used(bool) / created_at(autodate)；全部规则 = null（仅管理员读写）；索引 idx_email_codes_email
- `users.createRule` 收紧：`""`(公开注册) → `null`（仅管理员建号，注册全部走 worker 验证码流程）

**worker.js：**
- 新增 `/api/auth/send-code`：purpose=register/reset 分流（注册=邮箱必须未注册，重置=必须已注册）、60s 同邮箱冷却、**自建 SMTP 代理投递**（`env.MAIL_API` 可覆盖，默认 `http://mail.blupure.cn/mail/send`，POST JSON {token,to,subject,html}；验证码 worker 自生成，品牌化 HTML 模板）、验证码仅存 PB 服务端、返回 `{sent:true}` 不含 code、发送前作废旧码
- ⚠️ 原 ygwyy.top 公益接口已废弃（实测全部品牌化参数无效：发件人写死 KittenSpace、正文默认模板）
- 新增 `/api/auth/register`：校验 email/code/password/username → 校验码（未用+未过期+purpose=register）→ 先标记 used（防并发重放）→ admin 建号（verified=true）→ auth-with-password 签发 token
- 新增 `/api/auth/reset-password`：校验 code（purpose=reset）→ 标记 used → PATCH 新密码 → 签发 token 自动登录

**index.html：**
- 注册表单：验证码输入 + 「发送验证码」按钮（60s 倒计时重发）
- 「忘记密码？」入口 → 找回密码模式（邮箱+验证码+新密码）
- 三态 auth UI（登录/注册/重置）：renderAuthUI()/toggleMode()/enterResetMode() 集中管理

**验证：** auth_code_probe 40/40（mock 邮件 + mock PB + worker 直跑 + UI 6 段 A-F 全绿）、production_auth_verify 12/12（生产真实闭环：发码→注册→邮箱/用户名登录→重置→新密码登录→清理）、全量 runner 37/37

**服务器侧（发信代理，47.238.246.167）：**
- `/opt/mail-agent/mail_agent.py`（systemd mail-agent.service，监听 127.0.0.1:9527）：POST /mail/send {token,to,subject,html} → smtp.163.com:465，**双邮箱轮询**（YunTunTunWenYou / YunTunTunGame @163.com），发件人显示名「云吞吞文游」
- nginx：mail.blupure.cn:80 → 9527（DNS 灰云 A 记录，勿开橙云否则被 CF Worker 拦截）
- 维护：SSH root@47.238.246.167（凭据见本地记忆）

---

## 一、Cloudflare Workers 部署

### 1. 部署 worker.js（本次修改）
- 本次改动：`/api/cards/community` 支持 `?sort=play_count`（最热榜按游玩数倒序，缺省保持 `-created` 向后兼容）
- 方式：CF 控制台 → Workers → my-bitlife-ai（或 ai.blupure.cn 绑定的 worker）→ 粘贴更新 / `wrangler deploy`
- wrangler.toml 注释中的旧 Secrets 说明（PAY_APP_ID 等）已过时，可顺手更新

### 2. Secrets 完整清单（CF 控制台 → Worker → 设置 → 变量与机密）

**新增（本轮及 M1-M5 以来未配的，必须添加）：**

| 密钥名 | 值 |
|---|---|
| `XFSPARK_X2_KEY` | ⚠️ 见本地记忆/CF 面板（不在仓库保存明文密钥） |
| `XFSPARK_X2_FLASH_KEY` | ⚠️ 见本地记忆/CF 面板（不在仓库保存明文密钥） |
| `OPENROUTER_KEY` | ⚠️ 见本地记忆/CF 面板（不在仓库保存明文密钥） |
| `AGNES_KEY` | ⚠️ 见本地记忆/CF 面板（不在仓库保存明文密钥） |
| `H5_APP_ID` | `2608251350` |
| `H5_APP_SECRET` | ⚠️ 见本地记忆/CF 面板（不在仓库保存明文密钥） |
| `PAY_NOTIFY_URL` | `https://ai.blupure.cn/api/pay/notify` |

**已有/确认存在（不用动，核对即可）：**
`PB_URL`（http://db.blupure.cn）、`PB_ADMIN_EMAIL`、`PB_ADMIN_PASSWORD`、`ZHIPU_KEY`、`CHATANYWHERE_KEY`、`SILICONFLOW_KEY`、`DEEPSEEK_KEY`、`NVIDIA_KEY`、`MAPAY_API_URL`、`MAPAY_APPID`、`MAPAY_APPKEY`、`MODEL_URL_OVERRIDE`（可选，测试用）

**移除（旧易支付已废弃）：**
`PAY_APP_ID`、`PAY_APP_SECRET`、`PAY_MAPI_URL`、`PAY_QUERY_URL`

> MODEL_POOL 读取对照：XFSPARK_X2_FLASH_KEY(tier1)/XFSPARK_X2_KEY(tier2)/ZHIPU_KEY(tier3,7)/AGNES_KEY(tier4)/OPENROUTER_KEY(tier5,6,8,9)/CHATANYWHERE_KEY(tier10,20,30,95 enabled:false)/DEEPSEEK_KEY(tier40 disabled)/NVIDIA_KEY(tier90 disabled)/SILICONFLOW_KEY(兜底)

---

## 二、PocketBase（db.blupure.cn）—— 本轮无新字段

### 1. users 集合：确认/新增字段
- 已有：coins（Number，M1 修复后生产实际字段）
- 需新增（3 个）：
  - `last_checkin_date` Text（签到日期）
  - `checkin_streak` Number（连签天数）
  - `signature` Text（个性签名）
- 确认存在（会员/登录用）：`membership_type`、`membership_expires_at`（若后台没有则补 Text）

### 2. 需新建的集合（11 个，完整字段类型定义）：

> 通用说明：
> - 所有 `created_at` 字段用 **Autodate（仅创建时）**——worker 会手动传 ISO 字符串但 PB 自动忽略，由系统自动填充，`sort=-created` 排序正常
> - 去重由 worker 查询+幂等逻辑保证，**无需** unique 索引
> - 集合权限：列表/读取 = 公开，创建 = 登录用户，更新/删除 = 仅本人 + 管理员

**① posts（社区动态/帖子）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| content | Text | ✓ | max 500（worker 截断） |
| author_id | Text | ✓ | 发帖人 |
| card_id | Text | — | 引用剧本卡，max 64 |
| image_data | Text | — | 帖图 base64（前端压到 ~640px JPEG，worker 截断 400000，Text 无长度限制） |
| likes_count | Number | ✓ | 默认 0 |
| comments_count | Number | ✓ | 默认 0 |
| created_at | Autodate | — | 创建时 |

**② post_likes（动态点赞）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| post_id | Text | ✓ | 帖子 ID |
| user_id | Text | ✓ | 点赞人 |
| created_at | Autodate | — | 创建时 |

**③ post_comments（动态评论）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| post_id | Text | ✓ | 帖子 ID |
| user_id | Text | ✓ | 评论人 |
| content | Text | ✓ | max 200（worker 截断） |
| created_at | Autodate | — | 创建时 |

**④ follows（关注关系）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| follower_id | Text | ✓ | 谁关注 |
| user_id | Text | ✓ | 被关注者 |
| created_at | Autodate | — | 创建时 |

**⑤ unlocks（剧本卡解锁记录）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| user_id | Text | ✓ | 解锁人 |
| card_id | Text | ✓ | 解锁的卡 |
| created_at | Autodate | — | 创建时 |

**⑥ community_cards（社区作品，核心）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| title | Text | ✓ | 作品标题 |
| category | Text | — | 题材分类 |
| theme | Text | — | 主题（dark/light/minimal） |
| data | JSON | ✓ | 整卡数据（structured 全形） |
| author_id | Text | ✓ | 作者 |
| status | Select | ✓ | pending / approved（审核） |
| play_count | Number | ✓ | 默认 0，游玩数（详情页 +1） |
| earned_plays | Number | ✓ | 默认 0，已计佣游玩轮数 |
| unlock_count | Number | ✓ | 默认 0，解锁次数 |
| daily_plays | JSON | — | {userId: 当日次数}，防刷 |
| created_at | Autodate | — | 创建时 |

**⑦ card_likes（作品点赞）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| card_id | Text | ✓ | 作品 ID |
| user_id | Text | ✓ | 点赞人 |
| created_at | Autodate | — | 创建时 |

**⑧ card_collects（作品收藏）** —— 字段同 card_likes：card_id / user_id / created_at

**⑨ donations（打赏作者 + 送笔芯）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| card_id | Text | ✓ | 作品 ID |
| user_id | Text | ✓ | 打赏人 |
| author_id | Text | — | 被打赏作者 |
| amount | Number | ✓ | 打赏 10/50/100；送笔芯固定 1 |
| role_id | Text | — | 空 = 打赏作者；非空 = 送笔芯（记入角色人气） |
| created_at | Autodate | — | 创建时 |

**⑩ reviews（作品讨论区，嵌套回复）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| card_id | Text | ✓ | 作品 ID |
| user_id | Text | ✓ | 评论人 |
| content | Text | ✓ | max 200（worker 截断） |
| parent_id | Text | — | 空 = 顶层评论；非空 = 回复（嵌套） |
| created_at | Autodate | — | 创建时 |

**⑪ character_favorites（人物卡收藏）**

| 字段 | PB 类型 | 必填 | 说明 |
|---|---|---|---|
| card_id | Text | ✓ | 所属作品 |
| character_id | Text | ✓ | 人物 ID（npc id） |
| user_id | Text | ✓ | 收藏人 |
| created_at | Autodate | — | 创建时 |

### 2b. 无需创建的集合（说明）
- `_superusers` = 新版 PB 内置超级管理员集合，**自动存在**（管理员登录入口）
- `admins` = **旧版 PB 才有**，worker 代码只是降级兜底（`_superusers` 失败才尝试），新版环境直接跳过，**不用建**
- `pay_orders`（order_no/user_id/plan_id/amount/status/trade_no/created_at/paid_at）、`model_usage`（usage_date/model/…）——线上支付/token 计费已在跑，确认存在即可

### 3. PB 规则建议（保守）
- 新建集合全部：创建/读取=登录用户，更新=仅自己，列表读取=公开只读（与现网其他集合一致即可）

---

## 三、GitHub Pages（bitlife.blupure.cn）—— 全目录上传

### 必须上传（本次变更核心）：
| 文件/目录 | 说明 |
|---|---|
| `index.html` | 本次修改（发现页轮播/榜单/排序 + 三步创作向导 + 骨架模板市场） |
| `scenarios/` **全部 46 个文件** | 23 个 category_*.json + 21 个 scenarios.part*.js + scenarios.js + **skeletons.js**。⚠️ **skeletons.js 是本次新增，漏传 = 模板市场空白** |
| `icons/` 全部 20 个 | 官方 SVG 9 个(heart/heart-fill/chat/pen/user-plus/gift/star/grid/eye/trophy) + 登录/支付 4 个(qq/wechat/wechat-pay/alipay) + favicon/pwa 7 个 |

### 已部署过、确认在线上即可：
`CNAME`、`site.webmanifest`、`lib/qrcode.min.js`、`pay/alipay.png`、`pay/wechat.png`

---

## 四、第三方后台配置

1. **h5zhifu.com**：notify URL = `https://ai.blupure.cn/api/pay/notify`；⚠️ 严禁轮询查单接口（500 次/天黑名单）
2. **mapay（社交登录）**：redirect_uri 固定 `https://bitlife.blupure.cn/?social_cb=1`（worker 已写死，后台白名单需含此域名；2026-08-25 曾因域名不匹配报 103）

---

## 五、验证顺序（部署后）

1. worker.js 部署 → `curl https://ai.blupure.cn/api/health`（若有）或登录接口 200
2. PB 集合建完 → 用测试账号走一遍签到（确认 coins 入库）
3. GitHub Pages 上传 → 打开 https://bitlife.blupure.cn 确认：模板市场 8 骨架、发现页轮播/榜单、向导三步
4. 探针绕过 mock 直测生产（后续轮次做）

---

## 六、批9.5 轮次记录（2026-08-27）

**改动范围**：仅 `index.html`（worker.js 未动，Worker 版本沿用现有）
**commit**：`0c28a4c`（前端 GitHub Pages 自动上线）

修复 4 bug + 2 需求：
1. **资料持久化**：saveProfile 引用 PB_URL/COLLECTION 时作用域错误（定义在 AuthService IIFE 内）→ ReferenceError 被静默吞掉、PATCH 从未发出。修复为 saveProfile 内自行声明常量 + 成功后 AuthService.refreshCache() 落盘。头像上传统一压缩为 webp（compressImageFile：canvas 256px cover → toBlob 0.8，降级原图）
2. **清除缓存白名单化**：clearLocalCache 只删 `bitlife_save*` 前缀 + 10 个精确进度 key（最近游玩/map_override/selected_card/scenario_cards 等），保留 pb_auth_token/email + bitlife_user_cache_v1
3. **大地图**：pin 包裹进 `.fullmap-stage`（position:relative width:100%）+ clamp 6-94% 留边（translate(-50%) 不再漂出图片）+ max-width/ellipsis 兜底长地名；placeClick 点地点必关图，无 desc 直接 moveToLocation 并 switchView('view-main')
4. **AI 卡死**：requestAI finally 无条件复位 isGenerating + setSendingDisabled；**根因补充**：setSendingDisabled 对缺失元素（send-btn/btn-next-month 未渲染时）抛 TypeError，位于 try 之外 → isGenerating 卡死。加 null 保护
5. **NPC 首字头像**：8 处 emoji 头像 → firstLetterAvatar（名字首字+哈希底色）：聊天/档案/角色榜/打赏榜/作者/社区卡
6. **设置页精简**：删「叙事控制」「主题与展示」区块（保留 nc-route 访问线路+地图 override 游玩入口），「我的剧本」移至「最近游玩」下

**验证**：Playwright 探针 P1-P6 全绿（P1 持久化+webp / P2 白名单 / P3 地图 / P4 复位 / P5 首字 / P6 设置页）+ 语法检查通过 + sim-worker 26/26

---

## 七、支付网关多节点（2026-08-28）

**改动范围**：仅 `worker.js`（Worker 版本 `2386fbe9`，commit `fe9010f`）

**需求**：小徐提供 h5zhifu 国内多地域备用域名（默认 `open.h5zhifu.com`；北京 `bj.open.serverapi.work` / 上海 `sh.open.serverapi.work` / 广州 `gz.open.serverapi.work` / 成都 `cd.open.serverapi.work`），服务器网络不佳时避免支付下单失败。

**实现**（createPayOrder）：
- 新增常量 `PAY_GATEWAY_BASES`（默认域名 + 4 备用节点）
- 下单请求按序尝试：**网络失败 / HTTP 非 2xx → 自动切换下一节点**；**HTTP 2xx 但业务错误（签名等，code!==200）→ 直接报错不切换**（换域名无意义）
- `env.PAY_API_URL` 显式配置时只走该域名（禁用自动切换，供极端场景固定节点）
- 超时 15s/节点，最多 5 节点 × 15s ≈ 75s 兜底（前端 20s 超时兜底时先提示重试）

**验证**：本地单测（mock fetch 走真实 /api/pay/create 路由）T1 网络失败→切北京成功 / T2 HTTP500→切上海成功 / T3 业务错误不切换 / T4 全挂 502 / T5 显式 URL 单域名，全绿；`node --check` 通过；wrangler deploy 成功（Version `2386fbe9-bd97-43aa-ba68-5dba15e02c5a`）；线上 `POST /api/pay/create` 未登录返回 NOT_LOGGED_IN（路由存活）。

**注**：支付宝前端仍为"尚未开通支付宝支付"拦截（commit `d069b82`），本次仅加网关节点切换，未开通支付宝；微信支付走同一网关同样受益。

---

## 轮次 2026-08-28 晚 · 12 项大修第 1 批（commit f354c6f，仅 index.html）

**需求一** 状态面板四图标换下载 SVG：svg-search skill 下载 Material Symbols outline（monitor-heart/mood/diamond/workspace-premium，存放 F:\temp\svg\stat_*.svg）→ STAT_ICON_SVG 新增 4 个 emoji 键（😊💎👑）+ 4 个中文 label 键（健康/心情/魅力/权势），statBarHtml 查找链 icon→label→星标兜底；默认 stats 数据 icon 统一为 emoji（健康❤️/心情😊/魅力💎/权势👑）。
**需求二** 删加号菜单 红包/转账/礼物（HTML 按钮 + wxActionTap 分支 + names 映射），保留 相册/拍摄/语音通话/位置。
**需求三** 事件页搬世界页：删 #play-tabs tab + switchPlayTab + 顶层转发 + Controller export；#play-event-area 移入 view-world（图谱 card → 事件区）；事件区内 地图条 → 事件列表；剧情页只留 状态面板+聊天区；renderWorld 末尾渲染事件列表。
**bug1** 热聊榜错位：localNpcs 改 first-wins（与点开 resolvePersonCardNpc 语义一致）→ 榜名=官方卡第一个 NPC（la_01 陆烬川）。
**bug2+8** 折叠展开按钮：新建 #top-nav-expand-btn（header 外兄弟元素，.header.collapsed ~ 显示），删 .header.collapsed .top-nav-collapse-btn fixed 规则；桌面(≥521px) right:max(12px,calc(50vw-560px+12px)) 内容区右上，移动端 right:8px。
**bug3+4** 个人档案：头像 isAvatarImg+assignNpcAvatar 链（avatarColor 改 UIRenderer.avatarColor 防作用域崩）；小字 word-break:break-word+overflow-wrap:anywhere 完整显示。
**bug5** 事件名取章节标题：texts[0] 解析 card-title/wy-header/wy-role-name，失败回退 stripHtml 摘要。

**验证**：新探针 pv_bigfix_b1.py P1-P8 全绿零 pageerror；回归 pv_batch9_12_fix6/fix3/pv_mini_fixes 全绿；node --check 通过；push→SSH pull Fast-forward；线上抽查 19 项全过（PAY_GATEWAY_BASES 属 worker.js 不查 index.html）。
**待办**：第 2 批 bug6 结构化卡片（{{标题},{时间},{正文},{选项1},{选项2},{选项3}} 字段级流式+双轨兼容）+ bug7 角色聊天隔离（_personChat 生命周期）；第 3 批 BGM 2.0（7 风格名 mp3 + 题材映射 + 官方卡 bgm 字段 + 单曲循环）。

---

## 轮次 2026-08-28 深夜 · 12 项大修第 2 批 + 角色卡聊天解锁云存档（commit a1a564a，index.html + worker.js + scripts/pb_setup_collections.mjs）

**bug6 剧情卡片结构化重构**：
- AI 输出协议改为固定 6 字段 `{{标题},{时间},{正文},{选项一},{选项二},{选项三}}`，AI 不再输出 HTML；前端固定多主题模板（fillCardTemplate 填空 + themeClass），时间字段由 AI 输出
- 字段级流式渲染：StructuredCardProtocolService parseStreaming 按序非贪婪解析，每个字段流式 append（打字机），字段切换上个节点定型，正文含 `}` 由提示词规避+解析失败回退
- 双轨兼容：`{{` 开头走新结构，HTML/纯文本走旧路径（wrapAsHtmlCard 兜底）；老存档 renderHistory 正常
- 结构化强制重试（enableHtmlCardForce 时校验 6 字段完整性，缺则重试，复用重试计数）；结构化输出跳过 StateAuditorService（省 1 次 AI 调用）
- 历史存卡片 HTML（非协议串），renderHistory 还原正常

**bug7 角色聊天与剧本隔离**：`_personChat` 生命周期修复——openWechatChat/backToWechatList 均置 null，sendNpcMessage 双重校验（`_personChat && person:: 前缀`），剧本聊天不再串进角色卡存储；存量记录保留

**新功能 角色卡聊天 200 云币解锁 + 云端存档**（小徐确认：所有角色卡统一 200 币、终身会员免费、永久解锁不追溯、聊天记录上云跟随账户）：
- worker.js 新增 4 端点（Worker 版本 `f93de975`）：GET/POST `/api/char-chat/unlock`（幂等；isMember 免费；不足 402 INSUFFICIENT_COIN）、GET/POST `/api/char-chat/messages`（未解锁 403 CHAT_LOCKED；upsert 按 id；2MB 上限）
- PB 新集合：`char_chat_unlocks`（user_id/card_id/npc_id + autodate）、`person_card_chats_v1`（+ messages json 2000000 size）——pb_setup_collections.mjs 幂等脚本已建表并 git add -f 入仓
- 前端：openPersonCardChat 未登录→"请先登录"；本地存量记录→不追溯直进+迁移上云；GET unlocked→确认弹窗（200 币/免费）→POST 解锁→进聊天；云端记录优先渲染；savePersonChats 双写本地+云端

**验证**：pv_bigfix_b2 P1-P7 全绿零 pageerror（字段级流式/全链路/HTML兼容/JSON兼容/重试兜底/历史重渲染/隔离）；pv_char_chat_unlock P1-P7 全绿（未登录/确认/已解锁/存量迁移/取消/币不足/云端优先）；pv_mini_fixes P1-P9 + pv_batch9_12_fix6/fix3 回归全绿；node --check 通过；push→SSH pull Fast-forward；wrangler deploy 成功；线上抽查：POST /api/char-chat/unlock 与 GET /api/char-chat/messages 未登录均 NOT_LOGGED_IN（路由存活）、PB 两集合在线（6/7 字段）、bitlife.blupure.cn/index.html 含解锁 modal（6 处命中）
**待办**：第 3 批 BGM 2.0（git rm bgm_1~7 → 7 首风格名 mp3 → 题材映射 → scenarios.js 官方卡 bgm 字段 + schema 白名单 + 重建 → 单曲循环）

---

## 轮次 2026-08-28 深夜 · 12 项大修第 3 批 BGM 2.0（commit 5cc0ad3，index.html + scenarios/bgm/* + scenarios.js/part2~22 + scripts/build-scenarios.mjs）

**BGM 重命名换血**：git rm 旧 bgm_1~7.mp3（git 识别为 7 个 100% rename，无损）→ 风格名入仓：bgm_都市轻快恋爱风/宏大叙事钢琴纯音乐/朦胧梦幻科幻/悬疑诡异节奏/沉重氛围节拍/温柔感动钢琴曲/暧昧沉浸节拍；index.html BUILTIN_BGMS 改风格名 title+url。

**题材→曲目映射**（build-scenarios.mjs CATEGORY_BGM，23 题材 → 7 曲，重建 scenarios.js+part2~22 全 115 卡带 bgm 字段）：
- 轻快恋爱风：恋爱/都市/校园/职场/主播/娱乐圈/同人/经营（8 类，40 卡）
- 宏大叙事钢琴：历史/宫斗/修仙/女尊/穿越/跑团（6 类，30 卡）
- 朦胧梦幻科幻：科幻/奇幻/人外（3 类，15 卡）
- 悬疑诡异节奏：悬疑/无限流（10 卡）；暧昧沉浸节拍：ABO/乙女（10 卡）
- 沉重氛围节拍：末世（5 卡）；温柔感动钢琴曲：种田（5 卡）；无匹配→默认第一首

**前端**：bgmRebuild 初始进入（idx<0）时当前卡 bgm 字段 title 匹配定位曲目，无则第一首，播放中不打断用户选择；ended 单曲循环（currentTime=0+play，不再自动切下一曲），⏭/⏮ 手动切换保留（_switchTrack 淡出链不变）。

**验证**：探针 pv_mini_fixes 更新后 P1-P9 全绿零 pageerror（P3 新增：7 风格名/ABO 卡→暧昧沉浸节拍 idx6 定位/ended 同曲重播 currentTime 归零+src 不变+playing 保持/⏭ 切换；P3b 淡入淡出+gain 均衡+缓存命中+不存在回退全过）；回归 pv_bigfix_b2 P1-P7、pv_char_chat_unlock P1-P7、pv_batch9_12_fix6/fix3 全绿；node --check 通过；push→SSH pull；线上抽查：7 首风格名 mp3 全部 200（大小与本地一致）、旧 bgm_1.mp3 404（无残留）、scenarios.js 含 bgm 字段（首片 10 处）、index.html 含风格名。
**至此 12 项大修 3 批全部上线**（f354c6f 第1批 8 项 + a1a564a 第2批 bug6/bug7 + 角色卡聊天 200 币解锁云存档 + 5cc0ad3 第3批 BGM 2.0）。

---

## 轮次 2026-08-28 早 · 7 项修复（commit 53eed3a，index.html + scenarios/category_lishi.json + scenarios.part14.js + scripts/build-scenarios.mjs）

**①历史/发动态按钮无响应（同根因两处）**：
- 6 处裸 `escapeHtml()`（Controller IIFE 内，函数定义于 MarkdownService）→ 全部 `MarkdownService.escapeHtml(...)`（renderPostRefPicker 18530×2/18536、renderHistoryPanel 18939/18942/18943）
- renderPostRefPicker 裸调 `loadHomeOnlineCards()`/`_homeOnlineCards`（定义于 UIRenderer IIFE）→ ReferenceError 致发动态弹窗引用卡列表空白；修复：UIRenderer return 暴露 loadHomeOnlineCards + 新增 getHomeOnlineCards（10000），Controller 侧改 `UIRenderer.loadHomeOnlineCards()/getHomeOnlineCards()`（踩坑：曾误加进 ScenarioCardViewService return → IIFE 执行时 ReferenceError → 全服务 TDZ，diag 定位 12860:419 后修正）

**②状态面板 8 个 stat 专属 SVG**：STAT_ICON_SVG 补 4 组双键（🌀/灵力=auto-awesome、📚/学识=school、💼/事业=work、🌱/成长=eco，svg-search 下载 Material Symbols outline）+ ❤️ 换 monitor-heart path；8 stat 渲染 path d 全部不同，未知 stat 仍 ✨ 兜底

**③发现页指南针越界**：原手写指针 path 顶点 (4,20) 距圆心 11.31 > 圆半径 9 → 换 tabler:compass 下载版（圆 M3 12a9 9 + 刻度 + 指针 3 path，全顶点 ≤9）

**④默认回复正文量**：7 处 prompt 文案 400/600-800 字 → 不少于 300 / 300-500 字（getResponseRules/buildNarrativePrompt wordMin 300 wordMax 500/getIntroCoreRules/模板/buildBeautyRules/JSON 示例/结构化重试）

**⑤开局第一张卡时间剧本化**：实测 115 卡 timeline.start 仅 ls_01 缺失（year=-221 被 schema y>=0 拒绝静默变 2026）→ category_lishi.json 改 year 221 + note 注明公元前；build-scenarios.mjs 加 CATEGORY_YEAR 题材→年份映射（现代 2026/科幻末世 2200/ABO 2035/修仙奇幻人外跑团 1，源 JSON 缺 timeline.start 时注入，makeCard 2026 兜底保留）；重建 scenarios.js+part2~22 全 115 卡 timeline 完整

**⑥收藏图标反了+无提示**：左滑按钮 ☆/★ 文本 → 空心星（未收藏）/实心星（已收藏）SVG + `.on` 类（CSS 4005 启用金色变体）；toggleCollectLocal 成功分支加「收藏成功/已取消收藏」提示（带卡名）

**验证**：新探针 pv_batch3_fixes.py P1-P5 全绿零 pageerror（历史面板/发动态弹窗 10 卡/8 stat path 各异非✨/指南针 maxDist=9/收藏空心→实心+.on+toast 双向）；回归 pv_mini_fixes P1-P9、pv_bigfix_b2 P1-P7、pv_char_chat_unlock P1-P7、pv_batch9_12_fix6/fix3 全绿；node --check 通过；push→SSH pull；线上抽查 12 项全过（新 path 8 处、getHomeOnlineCards、300-500/不少于300 无 600-800、part14.js ls_01 start year 221 月9 日1）

---

## 轮次 2026-08-28 · 主角身份表单修改全链路生效（commit e0a8229，仅 index.html）

**背景**：游玩页初始角色信息弹窗（#init-preview-modal）修改姓名/性别/年龄后，剧情仍按剧本预制身份生成。根因：表单写回 state 正确，但生成链路 4 处读卡数据预制身份 + prompt 措辞「设定文本为唯一世界规则源」压制事实锁定。

**四处修复**：
- **A. buildQuickRef(card, state=null)**：身份行优先用运行时 `state.name/gender/age(约N岁)`，state 无效才回落预制；纯文本回退头部注入【身份】行并跳过原身份段提取（防双份矛盾）；后续轮调用点（userMsgCount>1）传 state
- **B. 新增 applyProtagonistOverride(text, state)**（ScenarioSpecCompressService 导出）：首轮全量设定按行重建【主角身份】段——姓名/性别/年龄用 state 值，背景等其余行原样保留；state 无有效姓名或卡文本无该段时原样返回
- **C. buildFirstSceneFields 首卡正文替换**：story 中预制主角名 split/join 替换为表单名（NPC 同名时跳过防误伤台词）；无 first_scene 的卡走 buildFallbackIntroFields 本已用 state.name，无需改
- **D. buildSystemPrompt 措辞追加一行**：「主角的姓名/性别/年龄等运行时事实以【事实锁定】中的「主角」信息为准；设定文本【主角身份】与其冲突时，以【事实锁定】为准。」（世界规则仍以设定文本为准不受影响）

**明确不改**：仅本次游玩生效（不写回卡数据，下次开局仍弹窗填预制值）；性别人称「他/她」不替换；getScenarioTextForModel 其余 5 个调用点（角色卡聊天/结局评定/审计员等）本次不覆盖。

**验证**：新探针 pv_name_override.py 四断言全绿零 pageerror——P1 首轮 prompt 含「姓名：林小满」不含「姓名：伊恩」；P2 后续轮 prompt 含「【身份】林小满·女·约19岁」不含「伊恩」+ 单元级 buildQuickRef 同断言；P3 InitIntroService.start 首卡正文含「林小满」不含「伊恩」；P4 措辞「以【事实锁定】为准」保留且 300-500/不少于 300 字/无 600-800 不受影响；回归 pv_batch3_fixes P1-P5、pv_mini_fixes、pv_bigfix_b2、pv_char_chat_unlock、pv_batch9_12_fix3/fix6 全绿；node --check 通过；push→SSH pull；线上抽查：applyProtagonistOverride×3、措辞×1、scenarios.js 预制名「伊恩」保留（卡数据未改）

---

## 轮次 2026-08-28 · SEO 收录基础（commit d2faba2，index.html + robots.txt + sitemap.xml）

**背景**：浏览器搜索「云吞吞文游」搜不到。诊断：head 仅 title/favicon，无 meta description/og/robots；线上无 robots.txt/sitemap.xml（404）；SPA 内容 JS 渲染，百度对 JS 抓取弱。

**改动**：
- index.html head 补：meta description（20+ 题材 AI 互动剧情）、keywords（云吞吞/云吞吞文游/AI文游/互动小说等）、robots index,follow、theme-color、og:type/site_name/title/description/url
- 新增 robots.txt（Allow: / + Sitemap 声明）、sitemap.xml（首页 daily/priority 1.0）——nginx 静态服务直接生效

**验证**：node 结构检查（title/desc/og/robots/head 闭合全过）；push→SSH pull；线上 robots.txt/sitemap.xml 200、meta description 命中。

**待办（用户侧）**：百度资源平台 ziyuan.baidu.com 提交站点收录（需账号）+ 微信打开页面「复制链接」扩散；收录需数天~数周生效。

---

## 轮次 2026-08-29 · 三修：选项后自动滚底 + 卡片正文颜色统一 + 历史记录左滑删除（commit 2903a18，仅 index.html）

**背景**：小徐实测三个 bug——①游玩页点选项后不自动滚到最新消息；②历史记录卡无法删除；③AI 剧情卡正文颜色与开局预制卡不一致。

**修复一 · 选项后自动滚底**：
- 根因：`scrollToBottom` 用 `evt.style.display !== "none"` 判断事件区——play-event-area 从不设 display:none，恒真 → 总是滚动空的 event 区，剧情卡区永不滚底
- 修复：改为 `hasEventCards = !!evt.querySelector(".event-card")` 有事件卡才滚事件区，否则滚剧情卡区（9429-9444）
- 滚动监听补全：main-content 的 wheel 仅向上滚才取消自动跟随（向下滚/底部翻看不打断）；scroll/touchmove 标记用户上翻；play-chat-area/play-event-area 单独挂 scroll 监听（scroll 不冒泡）（16640-16660）

**修复二 · 卡片正文颜色统一**：
- 根因：AI 直出 HTML 卡正文带内联 `style="color:..."`，穿透主题 CSS 变量（预制卡统一 --ct-text）；.card 根节点也无 color 兜底
- 修复：.card 根规则加 `color: var(--ct-text)`；applyCardTheme 清理根内联样式后，追加遍历删除所有子元素内联 color（仅 color，不碰背景等）（7301-7312、1890 区域）

**修复三 · 历史记录左滑删除**：
- renderHistoryPanel 行结构改为 `.hp-row.swipe-row` + `.hp-main.swipe-main` 自定义手势（touch/mouse 双通道，dx<-24 左滑出删除钮，dx>24 收起）；点删除只过滤 `play_history_v1` 中匹配 type+id 的条目后写回并重渲染，不碰 state/存档/云（18999-19070）；swiped 状态下点行主体仅收起、不打开卡

**验证**：pv_scroll_fix（A 纯点选项/B 上滚后点/C touchmove 后点 → 全 atBottom）、pv_card_color（预制/结构化/AI 直出三路正文色统一 rgb(51,51,51)，内联 color 清零）、pv_history_swipe（P1 左滑出删除钮+右滑收起、P2 删 c2 留 c1/x1 且 state 未动、P3 swiped 点击只收起不打开卡）全绿零 pageerror；回归 pv_batch3_fixes / pv_mini_fixes / pv_bigfix_b2 / pv_char_chat_unlock / pv_batch9_12_fix3/fix6 全绿；node --check 通过；push→SSH pull；线上抽查 hasEventCards/color: var(--ct-text)/hp-row swipe-row/removeProperty("color") 四项全命中

---

## 轮次 2026-08-29 · 夸克等浏览器强制暗夜模式封面不可见（commit 62a54f2，仅 index.html）

**背景**：小徐实测——夸克浏览器自带暗夜模式（浏览器级强制暗化，非网站 dark 主题）下，所有卡片封面无法正常显示。

**根因**：页面声明 `color-scheme: light`（:root），Chromium 内核（夸克 U4 基于 Chromium 108+）的 Auto Dark Mode 规则：页面声明 light 且用户开暗夜 → 浏览器对整页做强制反色/压暗。封面浅色渐变（#ffe8c0→#e2b26b）、封面图、白色高光全部被反色 → 内容不可见。且 getComputedStyle 返回原始值，JS 无法检测，仅绘制层改变（Playwright 的 --force-dark-mode / Local State enable-force-dark 在本机 headless 均未触发，无法自动化复现真实反色，靠规范与业界实践确认）。

**改动**（CSS Color Adjustment 规范的拒绝强制改色声明）：
- `:root { color-scheme: only light; }`：声明页面自行适配配色，禁止浏览器强制反色（light 主题）
- `body[data-theme="dark"] { color-scheme: only dark; }`：dark 主题同理（注意：color-scheme 只认根元素，body 上的声明不生效——已由 JS 兜底）
- head 加 `<meta name="color-scheme" content="only light">`（部分 UA 仅读 meta）
- ThemeService.apply 同步 `document.documentElement.style.colorScheme = "only dark" | "only light"`（覆盖 :root 初始值，dark 主题时根元素真正生效 only dark；行内样式优先级最高，不依赖 :has 兼容）

**效果**：夸克暗夜模式下网站保持自身设计的 light/dark 配色，不再被强制反色；网站自己的深色主题（data-theme="dark"）依然可用。

**验证**：Playwright 验证 light 根 color-scheme="light only"、setMode('dark') 后="dark only"、切回="light only"、持久化恢复="dark only"，封面元素（10 卡/shine/emoji）渲染正常零 pageerror；node --check 通过；7 回归探针（batch3/history_swipe/card_color/scroll_fix/name_override/mini_fixes/bigfix_b2）全绿；push→SSH pull；线上抽查 meta/only light/only dark/js 兜底四项全命中。

**待验证（用户侧）**：夸克浏览器开暗夜模式实测封面显示。若夸克为自研滤镜式暗化（非 Chromium Auto Dark），需再评估针对性兜底。

---

## 轮次 2026-08-29 · 删除封面 scc-shine 渐变高光层（commit a62a93c，仅 index.html）

**背景**：小徐指出封面图上叠有 CSS 渐变效果图层，要求「原汁原味的图片」——彻底删除，不是条件隐藏。

**现状确认**：所有剧本卡（115 张）均为真实封面图（has-cover，本地与线上 200 正常）；`.scc-shine` 白色斜向渐变高光条（`linear-gradient(100deg, transparent 40%, rgba(255,255,255,0.5) 50%, transparent 60%)`）在 has-cover 时**一直叠加在图片上**（此前只隐藏 emoji，未隐藏 shine），暗夜模式下白色被反色后更明显；另有 1px 白色内描边（box-shadow inset）。

**改动**（彻底删除 1653 字符）：
- 删除 3 组 `.scc-shine` CSS 定义及 dark 变体（scenario-card-cover 卡片 / hb-cover 轮播横幅 / rank-cover 榜单）
- 删除 2 处 HTML 渲染 `<span class="scc-shine"></span>`（卡片列表 + 轮播）
- 保留 `.has-cover { box-shadow: none; }`（有图时去 1px 白描边）
- 无图卡兜底（渐变背景 + emoji）保留——CoverService.ensure 失败/无图时仍有底色，不空白

**验证**：node --check 通过；Playwright 断言 shineNodes=0、shineCssRules=0、有图卡 coverBg=url(...)、coverShadow=none、children 仅 scc-emoji，无图卡 9 张渐变+emoji 兜底正常，零 pageerror；回归 pv_batch3_fixes / pv_mini_fixes / pv_history_swipe 全绿；push→SSH pull；线上抽查 scc-shine 残留 0。

---

## 轮次 2026-08-29 · 封面透明度/滤镜防御（commit 4e58907，仅 index.html）

**背景**：小徐提出假设——夸克暗夜模式可能是把封面图片的透明度调整到全透明导致「渲染不出图」，要求最小化改动避免。

**改动**（一条规则，5 行）：
```css
.scenario-card-cover, .hb-cover, .rank-cover {
    opacity: 1 !important;
    filter: none !important;
}
```
覆盖三类封面容器（剧本库卡片/轮播横幅/榜单小图）。若浏览器（UA 样式或非 !important 的 inline 注入）压低元素透明度或加滤镜，!important 强制还原，图片原图直出。emoji 的 drop-shadow 在子元素上，不受影响。

**验证**：node --check 通过；Playwright 断言 computed opacity=1/filter=none、emoji drop-shadow 保留、零 pageerror；模拟浏览器注入 `style.opacity='0'` + `style.filter='brightness(0)'` 后 computed 仍为 opacity=1/filter=none（!important 兜底成功）；push→SSH pull 部署。

---

## 轮次 2026-08-29 · 封面图改 img 标签（commit 2d5ac44，仅 index.html）

**背景**：小徐指出——`background-image`（容器背景图）比 `<img>` 标签更容易被夸克暗色引擎「吃掉变黑消失」，且背景图方案防御难度更高，优先改成 `<img src>`。

**改动**：
- 剧本库卡片（.scenario-card-cover）与首页轮播（.hb-cover）容器内新增 `<img class="scc-img" alt="" loading="lazy">`
- CoverService.paint 改为：容器内有 `.scc-img` 节点时设 `img.src`；无 img 节点（如旧渲染路径）回退 `background-image`，双通道兼容
- CSS 新增 `.scc-img`（absolute inset:0 + object-fit:cover + 默认 display:none，has-cover 时 block），img 盖在容器之上、渐变底色保留作加载期兜底
- 防御规则扩展为 `.scenario-card-cover, .hb-cover, .rank-cover, .scc-img { opacity: 1 !important; filter: none !important; }`——img 直接受保护

**验证**：node --check 通过；Playwright 断言懒加载后封面 img src 正确、naturalSize 960x720 加载成功、display:block、opacity:1/filter:none、emoji 隐藏、渐变兜底保留、零 pageerror；回归 batch3/history_swipe/card_color/mini_fixes 全绿；push→SSH pull；线上抽查 4 项（scc-img CSS/HTML/paint img 分支/防御含 img）全命中。

---

## 轮次 2026-08-29 · 超时回退旧结构卡片修复（commit 49b43b8，仅 index.html）

**背景**：小徐实测——模型调用超时后页面生成了「应该完全删掉的旧结构格式」剧情卡片（标题 minimal / 当前回合 / 状态:待更新 / AI想法块×2 / 3 固定按钮「继续观察局势/与关键人物交流/推进当前主线」/ 底部提示「可直接点击上方按钮继续剧情」）。

**根因链路**：`streamCompletionWithFallback(...).trim() || "（AI想法：刚刚有点卡住了…）"`（15666）空内容回退纯文本 → 非结构化 → beauty 分支（15677）→ 结构化重试也失败 → `finalizeCardLayout` 兜底（13362）调用 `wrapAsHtmlCard`（6591，全局唯一调用点）→ 旧模板卡。连接超时 abort 走 catch 提示路径，不产卡片。

**改动**：
- 超时回退文本改 6 字段结构化协议 `{{剧情提示},{},{AI 暂时卡住了，请再对我说一次吧喵～},{},{},{}}` → structuredOk=true 直通新卡渲染（finishStreaming → fillStructuredCard），跳过 beauty 分支/重试/冲突检测
- `finalizeCardLayout` 兜底改为构造结构化协议卡（标题=titleHint、时间=nowText、正文 `{}` sanitize 成中文括号防协议切分破坏）——覆盖「AI 纯文本输出 + 重试失败」等一切落底场景
- 彻底删除旧模板：`wrapAsHtmlCard`/`buildChoicesHtml`/`getStableThemeName` + 常量 `BASE_STYLE`/`BASE_STYLE_DARK`/`EMBED_STYLE`/`DEFAULT_THEME_NAME_STABLE` + 3 处导出（6624/6618/7188）
- 保留 `DEFAULT_CHOICES`/`getDefaultChoices`（HtmlStructureService 兜底选项用）与 `getSkeletonHtml`/`buildSkeletonStateContext`（骨架锁用）

**验证**：node --check 通过；新探针 pv_timeout_fallback.py 4 项全过（P1 空内容→剧情提示卡无按钮、P2 纯文本兜底→剧情推进卡、P3 正常 6 字段结构化回归、P4 wrapAsHtmlCard 已删，零 pageerror）；回归 batch3/card_color/history_swipe/mini_fixes/bigfix_b2/batch9_12_fix3/fix6 全绿；push→SSH pull；线上 sha256 与本地一致、curl 抽查新回退文本 2 处命中/旧模板文案 0 处。

---

## 轮次 2026-08-29 · 结构化卡片协议升级：逗号 6 字段 → JSON 对象（六字段必填）

**背景**：小徐要求——旧协议 `{{标题},{时间},{正文},{选项1},{选项2},{选项3}}` 改为更适合 AI 输出、更好解析、更不易出错的数据结构格式，六个字段全部必填；另补充：options 数量不强制为 3。

**新协议**：`{{"title":"...","time":"...","story":"...","options":["...","..."]}}`（保留 `{{` 前缀，isStructured/流式早停零改动；JSON.parse 原生解析，字段顺序无关）

**改动**：
- `StructuredCardProtocolService` 重写：`parseFull` 容错两级（①`{{"title":...}}` 直接内嵌字段时补花括号还原合法 JSON ②`{{` 后/`}}` 前多余字符时裁剪到最外层 `{…}` 子串）；六字段必填校验——title/time/story trim 非空 + options 去空后至少 1 项（数量不限，不再强制 3）；`parseStreaming` 正则 `"key"\s*:\s*"((?:[^"\]|\.)*)` 截断鲁棒 + `jsonUnescape` 转义还原
- `renderStructuredStreamingFrame`：取数改 `parsed.title/time/story`；首帧挂载条件改任一非空（字段顺序不定）；选项保持骨架占位，完整 JSON 由 finishStreaming 整卡替换
- Prompt 教学 3 处（buildBeautyRules / buildMainUserPrompt prevStructured 分支 / buildStructuredRetryPrompt）：示例与说明改 JSON；「options=恰好 3 个」→「至少 1 个，通常 2-4 个，数量不限」；双引号转义规则明确
- 超时回退文本（15631）与 finalizeCardLayout 兜底（13325-13327）改为 JSON 协议构造（JSON.stringify 自动转义；兜底选项用 AI 提取项补通用项 继续推进剧情/查看当前状态/换个方向探索）

**验证**：node --check 通过；新探针 pv_timeout_fallback.py 8 项全过（P1 空内容→剧情提示卡、P2 纯文本兜底、P3 正常 JSON、P4 wrapAsHtmlCard 已删、P5 流式分片打字机定型、P6 单选项/5 选项数量不限渲染、P6c 空 title 兜底）；回归 batch3/card_color/history_swipe/scroll_fix/name_override/bigfix_b2/mini_fixes/batch9_12_fix3/fix6 九探针数据正常零 pageerror；其余 6 探针 stub 全部同步为 JSON 格式。

---

## 轮次 2026-08-29 · 流式骨架即时挂载 + 日记/事件源码清洗（commit 59828c0）

**背景**：两个线上反馈——①剧情卡片流式输出时用户看到源码一行行打出、最后才突然变成美化卡（期望：先渲染主题卡骨架，再实时填充内容文字）；②日记与事件模块又开始显示代码（残缺 JSON 协议源码经兜底路径进入正文/history 并在日记/事件原样展示）。

**改动**（index.html，32 insertions/6 deletions）：
- `renderStructuredStreamingFrame`（8916）：删除 `if (!parsed.title && !parsed.time && !parsed.story) return;` 守卫，`{{` 出现即挂载主题卡骨架（fillStructuredFrame），title/time 已定型直接写入，story 打字机逐段定型——杜绝「源码先渲染、终版才变卡」观感（P7 探针验证：骨架挂载瞬间 bubbleText 为空，无任何源码上屏）
- `finishStreaming` else 分支（9510）：新增残缺结构化 JSON 兜底——`{{` 前缀但六字段不齐/解析失败时，用 parseStreaming 提取已定型 title/time/story + 3 通用选项渲染兜底卡并替换旧节点，源码不上屏不入历史（原路径是 innerHTML 源码 bubble）
- `finalizeCardLayout` 兜底（13302-13313）：story 改从 parseStreaming 提取（原为整段源码进兜底卡正文）
- `buildEventRounds`（9064-9076）：对 `{{` 前缀历史文本清洗为 `【标题】正文`，日记/事件不显示协议源码（兼容残缺/旧数据）

**验证**：node --check 通过；pv_timeout_fallback.py 11 项全过（新增 P8 残缺 JSON stub → 兜底卡 title=雨夜剑鸣/story 提取/3 通用选项/noRawOnScreen/histIsHtml；P8b buildEventRounds 对 `{{` 源码清洗 t0=`【旧数据】\n旧数据正文`/t1 含完整正文不含 `"options"`）；回归 batch3/card_color/history_swipe/scroll_fix/name_override/bigfix_b2/mini_fixes 七探针全绿零 pageerror；push→SSH pull；线上 sha256 11020d1e9ad37a6c59d7221f713244f6415a5bf4b812738a0cfe7f238695415b 与本地一致、curl 抽查「残缺」5 处/parseStreaming(text) 3 处/旧格式 0 处。

---

## 轮次 2026-08-29 · SEO 可爬性增强（commit d627738）

**背景**：小徐反馈——除百度外，其他搜索引擎搜「云吞吞文游」也搜不到。诊断：robots.txt 放行、sitemap.xml 存在、title/description/keywords/og 齐全、sitemap 含首页，但 **站点从未被任何搜索引擎收录**（新子域名、无外链、从未向 Bing/Google 提交）；且 SPA 单文件应用静态正文几乎为空（仅 h1 标题与加载提示）、无 JSON-LD 结构化数据，无 JS 爬虫（百度/Bing）可索引内容极少。

**改动**（index.html，38 insertions）：
- boot-screen 内新增 `.boot-seo` 静态简介（品牌+玩法+题材+功能，约 90 字）：无 JS 爬虫直接可见（boot-screen 常驻），真实用户加载页可见 1-2 秒后淡出，零 UI 破坏
- head 新增 `application/ld+json` JSON-LD：WebSite + VideoGame 双实体（名称/URL/描述/题材 genre/免费 Offer/zh-CN）
- F:\temp\extract_scripts.py 增加 ld+json 块跳过（node --check 不受 JSON-LD 影响）

**验证**：JSON-LD 解析合法（WebSite+VideoGame）；boot-seo 渲染可见（DOM 检查 display/opacity/text）；主探针 11/11 全过零 pageerror；push→SSH pull；线上 sha256 f1555efa... 与本地一致，curl 抽查 boot-seo-text 2 处/ld+json 1 处/标题文案与 applicationCategory 命中。

**未做**：GitHub README 外链（小徐明确不允许——担心项目被抄袭）；百度收录（小徐已排除）。

---

## 轮次 2026-08-29 · 社区动态引用显示真实剧本名 + 签到文案去解锁引导（commit d224eef）

**背景**：①小徐反馈——社区动态页已发布的动态剧本引用显示内部 ID（「该动态引用了剧本「scenario_preset_la_05」」）。根因：worker /api/feed 的 card_title 仅从 community_cards 集合映射，官方预设卡（scenario_preset_la_xx）不在该集合 → 返回空 → 前端 fallback 显示 card_id；②签到成功提示含「去解锁剧本卡吧」引导，小徐要求去掉。

**改动**（index.html，12 insertions/3 deletions）：
- 新增 `feedCardTitle(p)` helper：`p.card_title` → `window.SCENARIO_LIBRARY` 按 id 映射标题（官方卡真实标题，如 la_05=青梅竹马的十年之约）→ 最后 fallback 原 id
- feed 列表引用渲染（18444）与 openFeedCard 弹窗（18787）改用 helper（弹窗保留 cardId 兜底）
- 签到成功弹窗文案「去解锁剧本卡吧～」→「去体验新剧本吧～」

**验证**：node --check 通过；新探针 pv_feed_ref.py 2/2（列表显示「青梅竹马的十年之约」无内部 ID、弹窗含真实标题+「可在剧本库查找后游玩」无 ID，零 pageerror）；主探针回归 11/11；push→SSH pull；线上 sha256 445217011efc... 与本地一致、grep feedCardTitle 3 处/新文案 1 处。

---

## 轮次 2026-08-29 · 社区动态清理机制 + 图片 WebP 化（commit dd8fb94 + wrangler deploy）

**背景**：小徐要求——①删除论坛现有全部动态；②每个动态 30 天后自动从服务器删除（避免长期占用存储）；③动态图片用 WebP 存储省空间。现状：posts 仅 1 条（当日测试动态「好甜🥰～」），image_data 为 base64 内嵌 DB（jpeg），长期使用会持续膨胀。

**改动**：
- **清空数据**：管理员 API 删除 posts（1 条）+ post_likes（1 条）+ post_comments（0 条），三个集合均归零（DELETE 204 无 body，验证按 totalItems 确认）
- **worker.js**：export default 新增 `scheduled` handler——每天删除 `created < now-30d` 的 posts（perPage 200 分批循环，先删关联 post_likes/post_comments 再删 post），失败不中断、记录 console
- **wrangler.toml**：新增 `[triggers] crons = ["10 4 * * *"]`（北京时间每天 12:10 执行）
- **index.html**：handlePostImage 压缩从 `image/jpeg 0.8` 改为 `image/webp 0.8`（最长边仍 640px；webp 平均比 jpeg 小 30-50%，现代浏览器/微信 X5 全支持）

**部署**：node --check 通过；git commit dd8fb94 → push → SSH pull（线上 sha256 8190022958... 与本地一致，image/webp 2 处）；worker 经 `npx wrangler deploy` 部署（Uploaded my-openai-proxy，Current Version af5838d8...，schedule: 10 4 * * * 触发器已应用）；curl 验证 ai.blupure.cn 正常响应（/api/pay/offer 401 预期）、站点 200。

**注意**：老动态图片（jpeg base64）随 30 天清理机制自动过期，无需迁移；新发帖图片一律 WebP。

---

## 轮次 2026-08-29 · 结构化协议改为标准 JSON 对象输出（commit 7ed947b）

**背景**：小徐实测 AI 返回 `{"title":...}` 标准 JSON 对象（裸 JSON，无 `{{}}` 包裹）不被前端解析，整段源码上屏。「直接让ai输出json咋了不行,你这不是给ai增加难度吗」→ 协议直接改为标准 JSON 对象，`{{}}` 仅保留兼容。

**改动（index.html）**：
- **StructuredCardProtocolService**：parseFull 支持两种前缀——`{{` 旧格式（补花括号容错）与裸 `{`（最外层 `{…}` 裁剪解析）；六字段校验（title/time/story 非空 + options≥1 非空）；isStructured 对裸 JSON 要求 parseFull 成功（防纯文本误判）；parseStreaming 增加 jsonUnescape 转义还原
- **renderStreamingFrame**：新增 `isBareJsonStream` 启发式 `/^\{\s*"(title|time|story)"\s*:/` → 裸 JSON 流式即挂骨架打字机（不等 `{{`）
- **finishStreaming**：fill 协议判定排除顶层含 `title` 键的 JSON（裸 JSON 结构化与 fill 填空协议区分，防残缺结构化误入 fill 卡「当前回合」）；残缺裸 JSON（`/^\{\s*"/` 命中）走兜底结构化卡提取已定型字段，杜绝源码上屏
- **appendMessage/patchLastAiMessage/重试链 turnFill**：同款 title 键排除
- **buildEventRounds**：清洗 `{{` 或 `/^\{\s*"/` 前缀源码为【title】story（日记/事件渲染端，兼容旧 `{{}}` 数据）
- **教学文案 3 处**（buildBeautyRules/buildMainUserPrompt/buildStructuredRetryPrompt）：示例改标准 JSON `{"title":"本回合场景名",...}`，「本次必须只输出一个标准 JSON 对象，禁止任何额外文字」「禁止用 ``` 或双花括号 {{ }} 包裹」；JSON 填空协议加区别说明
- **超时回退**（15699）：改裸 JSON `{"title":"剧情提示",...}`（3 通用选项）；**finalizeCardLayout 兜底**：JSON.stringify(fallbackObj) 不再包 `{{}}`；**重试早停**：`/\{\s*"(title|time|story)"\s*:/` 检测

**验证**：node --check 通过；主探针 pv_timeout_fallback.py 11/11（P1 空→剧情提示卡、P2 纯文本兜底、P3 裸 JSON 正常渲染、P4 `{{}}` 兼容、P5 裸 JSON 流式分片打字机、P6 单/5 选项、P7 骨架即时挂载源码不可见、P6_EMPTY_TITLE 兜底剧情推进、P8 残缺裸 JSON 兜底+历史 HTML、P8b 历史清洗裸 JSON+旧 `{{}}` 双格式）；回归 pv_batch3_fixes/card_color/history_swipe/scroll_fix/name_override/bigfix_b2/mini_fixes/batch9_12_fix3/fix6 全绿；push→SSH pull；线上 sha256 14867c1a... 与本地一致（管道哈希误报 e3643f66，落盘对比一致），grep 教学「本回合场景名」3 处/「标准 JSON 对象」4 处/双花括号教学 5 处。

**注意**：fill 填空协议（无 title 键、story 开头）与结构化裸 JSON（含 title 键）按顶层 title 键区分，两者互不误认。

---

## 轮次 2026-08-29 · 结构化输出升级：response_format json_object 约束（commit cfadfe6）

**背景**：小徐要求「最稳定的工业方案」根治 AI JSON 输出解析。实测模型池 Function Calling 兼容性后定稿 JSON Mode 方案。

**实测结论（线上 ai.blupure.cn，测试用户 tools_test@mapay.test lifetime）**：
- **Function Calling + tool_choice 强制**：非流式 GLM-4-flash/minimax-m2.7/lfm 完美；但**流式不可靠**——GLM 流式忽略工具输出文本 JSON（偶带 ``` 围栏）、讯飞流式先吐 reasoning_content 思考流再 tool_calls、OpenRouter 免费池流式断流（minimax-m2.7 零输出）。工具通道不配作流式主通道
- **JSON Mode（response_format: {"type":"json_object"}）**：讯飞 X2-Flash/GLM-4-flash/minimax-m2.7 非流式+流式全绿——输出干净裸 JSON（无围栏无解释），GLM 比 tools 模式更干净；讯飞 reasoning_content 前端已忽略（14454 行只取 content）
- 池内 503（GLM-4-air/agnes/ox-alpha）与模式无关（不带任何参数同样 503）

**改动（index.html）**：
- streamCompletion（14405）与 requestCompletion（14514）请求体恒带 `response_format: { type: "json_object" }`（模型原生格式约束，prompt 含「标准 JSON 对象」字样满足 OpenAI 规范）
- parseFull 开头 stripFence（```json 围栏剥除，容错3）
- isStructured / isBareJsonStream / finishStreaming 残缺判定 / buildEventRounds 清洗：全部容忍 ``` 围栏前缀（`^(```[^\n]*\n)?\{\s*"`）

**改动（worker.js）**：`model: "pool:<模型id>"` 可指定池内模型（认证后可用，兼容性实测后门，前端正常传 "pool" 不受影响）

**验证**：node --check；主探针 12/12（新增 P9_FENCE_JSON 围栏包裹裸 JSON 正常渲染）；pv_bigfix_b2 全过（含重试链/HTML 卡兼容）；请求体确认带 response_format（P4 抓包）；push→SSH pull；线上 sha256 0e799697 与本地一致、json_object 2 处。

**注意**：GLM-4-air/agnes/ox-alpha 当日 503 为池内临时故障，观察恢复；OpenRouter 免费池流式不稳是上游特性，自动 fallback 兜底。

---

## 轮次 2026-08-29 · 选项双序号修复（commit ebd4dbb）

**背景**：小徐反馈结构化卡片选项前出现两个序号（模板自带 ① ② ③ icon + AI options 文本自带「①拔剑相对」「1. xxx」前缀 → 「① ①拔剑相对」严重影响观感）。

**改动（index.html）**：StructuredCardProtocolService.parseFull 的 options 映射增加序号前缀清洗正则（覆盖 ①-⑩、数字+.)、）、、全角括号（1）、【1】、选项N：等 6 类前缀）；parseStreaming 的 options 来自 parseFull 自动继承；data-choice（点击发送给 AI 的文本）同一变量同步干净。fill 协议（7349）与 collectChoices/normalizeChoiceLabel（7696）原有清洗不动。

**验证**：node --check；主探针新增 P10_OPT_PREFIX（6 种序号前缀 stub → 渲染文本全剥净、断言精确等于无前缀文本）12/12 全过；pv_bigfix_b2 P2 的「①拔剑相对」为模板 icon+文本拼接（探针读 textContent 含 icon，非 bug）；push→SSH pull；线上 sha256 2014d148 与本地一致。

---

## 轮次 2026-08-29 · 选项序号 icon 扩至 10 项（commit 7491f95）

**背景**：小徐指出卡片模板自带 ① ② ③ 序号 icon，但选项可能超过 3 个（4 个或更多），icon 数组需预留到 9 个以上，否则第 4 个选项起 fallback 为阿拉伯数字、与 ①②③ 混排破坏观感。

**改动（index.html）**：fillCardTemplate（7408）与 fillStructuredCard（7519）两处 icon 数组 `["①","②","③","④"]`/`["①","②","③"]` 统一扩至 `["①","②","③","④","⑤","⑥","⑦","⑧","⑨","⑩"]`（10 项）；第 11 个选项起才回退 `String(i+1)` 数字（实际不会出现）。

**验证**：node --check；主探针 13/13 全过（P10 六选项 stub → optionIcons 断言 `["①","②","③","④","⑤","⑥"]` 精确匹配）；回归 9 组（batch3_fixes/card_color/history_swipe/scroll_fix/name_override/bigfix_b2/mini_fixes/batch9_12_fix3/batch9_12_fix6）全绿；push→SSH pull；线上 sha256 02830b84 与本地一致。

---

## 轮次 2026-08-29 · 讯飞门控放宽（commit 28ce1cb）

**背景**：小徐咨询模型池承载力与扩容。分析结论：全局瓶颈是 worker 自设门控（RATE_LIMIT 按 apiKeyEnv 分组 2 并发/12min）→ 全局 ~72 次请求/分钟，高峰期约承载 35-40 活跃玩家。讯飞官方并发 20 无日次硬限，自设 2 并发保守 10 倍 → 第一步放宽讯飞。

**改动（worker.js）**：新增 `RATE_LIMIT_OVERRIDES` + `rateLimitFor(envName)`：讯飞两个 key（XFSPARK_X2_FLASH_KEY / XFSPARK_X2_KEY）门控放宽至 **8 并发 / 60 次每分钟**（官方并发 20，留 2.5 倍余量）；其余平台（智谱免费档 ~5RPM、OpenRouter 免费模型 50 次/天、Agnes、SiliconFlow）保持保守默认 2 并发/12min 防 429。gateAcquire 改读 rateLimitFor。

**验证**：node --check；pool: 后门线上实测讯飞两模型 HTTP 200 通路正常；6 并发突发测试全 200（旧配置 2 并发下会排队/超时失败），耗时 10-19s 为深度推理生成特性非门控排队；push→SSH pull 到 28ce1cb。

**效果**：全局请求上限 72 → ~150+ 次/分钟（讯飞两通道为主力），承载翻倍。后续候选：启用 DeepSeek 官方（tier 40 已配置）、多 key 轮换、NVIDIA 额度复查。

---

## 轮次 2026-08-29 · NVIDIA 模型批量入池（commit f86e4d6 + 8b6f9c5）

**背景**：小徐指示「NVIDIA 免费模型都应该加进池子」。此前池内仅 nv-llama-3.3-70b（8-24 实测 12s+ 无响应疑似额度耗尽，禁用）。复查发现 key 已通：平台 /models 全量 83 个模型，筛选 21 个对话类候选（DeepSeek-V4 系列 / Kimi k2.6+k3 / minimax-m3 / nemotron 系列 / gpt-oss / mistral / gemma / phi-3.5 / yi-large）。

**改动（worker.js）**：MODEL_POOL 新增 21 个 NVIDIA 条目（enabled:false，dailyCap 100/天，tier 90 兜底）→ `pool:` 后门逐个实测 **20/21 全 200**（仅 llama-3.3-70b 已从平台下架 503，保留条目禁用防误测）→ 实测通过的 20 个 + 原 llama 下架，实际启用 **20 个**（enabled:true）。

**验证**：node --check；22 候选逐个实测 20 OK（4 并发，7-12s 为推理模型生成特性）；响应结构确认：NVIDIA 模型均带 reasoning_content（前端已忽略只取 content，流式通道正常）；nv-deepseek-v4-flash / nv-kimi-k2.6 实测 content 输出正常 JSON；部署后普通路由 200 + NVIDIA 抽测 200；push→SSH pull 到 8b6f9c5。

**注意**：NVIDIA 平台按 credits 计费，key 额度有限，dailyCap 100/模型/天为保守值；用完会 429 → 自动熔断 fallback，无需干预。

---

## 轮次 2026-08-29 · NVIDIA dailyCap 放宽（commit 0f7ea89）

**背景**：小徐指出未充值、只用 NVIDIA 免费模型，dailyCap 100 偏保守。查证 2026 NVIDIA Build 免费层政策：全模型免费（NVIDIA 承担成本），credits 体系已移除，改 per-model 隐藏限流（~40 RPM 大模型 / 1000 req/day 大模型、14400/day 小模型，429 带 Retry-After，无公开用量 API）。

**改动（worker.js）**：20 个 NVIDIA 模型 dailyCap 100 → 500（平台 ~1000/day 上限的 1/2 留余量，超限 429 → 已有 Retry-After 处理 + 熔断 fallback 链兜底）。

**验证**：node --check；push→SSH pull 到 0f7ea89。

---

## 轮次 2026-08-29 · NVIDIA 按平台限流精细化管理（commit 3b3eb6f）

**背景**：小徐要求按 NVIDIA 免费层真实限流规则（大模型 ~40 RPM/1000 req/天，小模型 ~60 RPM/14400 req/天，超限 429+Retry-After）调整调用规则，让每个用户体验最好。此前 NVIDIA 20 模型共享 key 级 gate（2 并发/12min）过于保守。

**改动（worker.js）**：
1. **模型级门控** `MODEL_LIMIT_OVERRIDES`：NVIDIA 每模型独立 gate（gate key = envName#modelId），大模型 3 并发/30min、小模型 5 并发/45min（平台上限的 75% 余量）；gateAcquire/gateRelease/rateLimitFor 支持 modelId 参数，调用处传 target.id；无覆盖的模型/平台走原 key 级逻辑不变
2. **dailyCap 区分**：12 个大模型（deepseek-v4/kimi/minimax-m3/nemotron 大系/gpt-oss-120b/mistral-large2/70b/ultra-253b）800/天；8 个小模型（nano/lightning/gpt-oss-20b/mistral-nemotron/gemma/phi-3.5/yi）5000/天（对应平台 1000/14400，取 ~80%/35%）
3. **429 短熔断**：`MODEL_429_COOLDOWN_MS=30s`，429 且 Retry-After 等待重试仍失败 → 短熔断 30s（下个请求直接跳过该模型，不白试）；5min 级失败熔断不变

**验证**：node --check；小模型 5 并发全 200 并行放行（旧门控会排队）；大模型 kimi-k3 200；普通路由（讯飞 tier1）200 不受影响；push→SSH pull 到 3b3eb6f。

---

## 轮次 2026-08-29 · 智谱/讯飞双 key 扩容（commit 7544b4f）

**背景**：小徐提供①智谱新注册账号 key（0a84a2ef...QY94taHgzKuCYbAj）②讯飞新授权凭据（APPID 1fabb653 + APIKey + APISecret + 系统默认 key，每模型 20w token/年）。

**讯飞鉴权排查**：APIKey/APISecret 走 HMAC 签名（WebSocket 协议）；HTTP OpenAI 兼容端点的有效 Bearer 是**「系统默认」key**（`CtzgALfxIcDjRInCUnvh:IalczgVRogHiSvrJcdHK`）——实测 9 种组合后确认。X2 端点（spark-api-open/x2）用该系统默认 key 实测 200 出内容；**X1.5/Pro/Pro-128K/Ultra-32K 无 HTTP 端点（404，仅 WebSocket）**，暂无法接入，额度留待 wss 客户端方案。

**改动（worker.js）**：
1. 智谱双 key：`zp2-glm-4-air`（tier 3）/`zp2-glm-4-flash`（tier 7）用新 ZHIPU_KEY2，插在旧条目前优先消耗新账号配额，旧 key 超限/失败后 fallback（gate 按 key 独立 2 并发/12min）
2. 讯飞 X2 新授权：`xf-spark-x2b`（x2 端点 + XFSPARK_X2_KEY2，tier 2 优先于旧 key，dailyCap 20/天——20w token/年额度约 50-100 轮剧情，429 后自然 fallback）

**Secrets**：wrangler secret put ZHIPU_KEY2 / XFSPARK_X2_KEY2（CF my-openai-proxy）✓

**验证**：node --check；pool: 实测 pool:xf-spark-x2b 200 出内容、pool:zp2-glm-4-air 200 出内容；wrangler deploy 到 1b3872ce；git push→SSH pull 到 7544b4f。模型池 36 启用。

---

## 轮次 2026-08-29 · 讯飞 HTTP 全服务接入（commit 8f0efd9）

**背景**：小徐补充讯飞 6 个 HTTP 服务（OpenAI 兼容）的完整认证信息：每个服务独立「系统默认」key（格式 `xxx:yyy`，Bearer 鉴权），Lite 无限量、其他每模型 <20w tokens/年。上一轮（7544b4f）只接入了 X2；本轮补齐 X1.5/Lite/Pro/Pro-128K。

**各服务实测结果**（直接 POST spark-api-open.xf-yun.com 验证）：

| 服务 | 端点 | model 名 | key | 结果 |
|---|---|---|---|---|
| Spark X (X2) | `/x2` | `spark-x` | WQHGCFVJdkalFMEQMCMo:qeFm... | ✅ 200 |
| Spark X1.5 | `/v2` | `spark-x` | 同上（X2 同 key） | ✅ 200 |
| Spark X2-Flash | `/agent/v1` | `spark-x` | Ctzg...(系统默认) | ✅ 200（原已接入） |
| Spark Lite | `/v1` | `lite` | AmtvrQwmvosLxEBkoDXj:SnPt... | ✅ 200 **无限量** |
| Spark Pro | `/v1` | `generalv3.5` | GxiGdQSiSyYiSlSzfaDY:JMiK... | ✅ 200 |
| Spark Pro-128K | `/v1` | `pro-128k` | uZBkZKxcnIIjNaAMyrFk:EYyP... | ✅ 200 |
| Spark Ultra-32K | 未知 | 17 候选全 invalid（ultra/spark-4.0/generalv4.0/spark-4.0-ultra-32k 等） | MNHV... | ❌ 400 暂缓 |

**改动（worker.js）**：
1. 新增 4 条目：`xf-spark-x1`（v2 端点，tier 2，dailyCap 20）、`xf-spark-lite`（v1+lite，tier 4，**dailyCap Infinity**）、`xf-spark-pro`（tier 5，dailyCap 20）、`xf-spark-pro128k`（tier 5，dailyCap 20）
2. RATE_LIMIT_OVERRIDES 增加 `XFSPARK_X2_KEY2`（8 并发/60min）与 `XFSPARK_LITE_KEY`（8 并发/60min）——官方并发 20，取 40% 余量
3. dailyCap 20 = 20w token/年额度 ≈ 50-100 轮剧情/天，超限自然 fallback 到同 tier 后续模型（Lite 无限量是主要后盾）

**Secrets**（wrangler secret put，4 个）✓：XFSPARK_X2_KEY2（WQHG，与旧 XFSPARK_X2_KEY 区分）/ XFSPARK_LITE_KEY（Amtvr）/ XFSPARK_PRO_KEY（GxiG）/ XFSPARK_PRO128_KEY（uZBk）

**验证**：node --check；wrangler deploy 到 972e081a；线上 pool: 实测 xf-spark-x2b/xf-spark-x1/xf-spark-lite/xf-spark-pro/xf-spark-pro128k 全 200 出内容；push→SSH pull 到 8f0efd9。模型池 41 启用。

**待办**：Ultra-32K 需从小徐控制台确认 model 名（HTTP 端点可能为 `/v3.1` 等特殊路径，17 候选未命中）。

---

## 轮次 2026-08-29 · 讯飞 Ultra-32K 接入（commit fcdb69d）

**背景**：小徐提供 Ultra-32K 的 HTTP 服务信息（`https://spark-api-open.xf-yun.com/v1/chat/completions` + MNHV 系统默认 key）。此前 17 个 model 名候选全 invalid（ultra/spark-4.0/generalv4.0/spark-4.0-ultra-32k 等）。

**查证**：抓取讯飞官方 HTTP 调用文档（www.xfyun.cn/doc/spark/HTTP调用文档.html）→ model 参数表确认 **`4.0Ultra`** 指向 Ultra 版本（六版本 Lite/Pro/Pro-128K/Max/Max-32K/4.0Ultra 独立计量）。文档还确认 Ultra 已升级 X1.5 快思考模式、FunctionCall 效果显著提升（仅 http 支持）——对剧情工具通道友好。

**改动（worker.js）**：
1. 新增 `xf-spark-ultra`（v1 端点 + model `4.0Ultra`，tier 4 **排在 Lite 前面**——Ultra 是最强非思考模型，20 次/天额度内优先用，超额自然 fallback Lite 无限量，dailyCap 20 ≈ 20w token/年）
2. RATE_LIMIT_OVERRIDES 增加 `XFSPARK_ULTRA_KEY`（8 并发/60min）

**Secrets**：wrangler secret put XFSPARK_ULTRA_KEY（MNHV...）✓

**验证**：node --check；直接 POST 实测 200 出内容（此前 17 候选全 400）；wrangler deploy 到 75ff7f76；线上 pool:xf-spark-ultra 200；push→SSH pull 到 fcdb69d。**模型池 42 启用（讯飞 8 模型全接入）**。

---

## 轮次 2026-08-29 · 讯飞额度保护（commit 9cb8399 + 4081133）

**背景**：小徐反馈控制台剩余额度——X2-Flash 剩 **1,723,155 tokens**（2M 免费包）、X2 系（xf-spark-x2 旧 key + xf-spark-x2b/x1 新 key）**三条目共享额度池合计仅剩 196,242 tokens**。此前 X2-Flash 与 xf-spark-x2 均设 dailyCap Infinity（误判为无日次硬限=无限量），实际为 token 配额制，Infinity 会无节制烧光共享额度。

**实测**：真实剧情请求（教学 system + 4 轮历史）约 **1000-2500 tokens/轮**（含 reasoning）。

**改动（worker.js）**：
1. `xf-spark-x2-flash`：dailyCap Infinity → **150/天**（≈40 万 tokens/天 ≈ 撑 4 天+，超额自动 fallback）
2. X2 系三条目：`xf-spark-x2`（旧 key）Infinity → **10/天**、`xf-spark-x2b`/`xf-spark-x1` 20 → **10/天**（每天合计 ≤30 次 ≈ 6 万 tokens ≈ 撑 3 天，优质额度降为补充角色）
3. 注释更新额度地图：X2-Flash 172 万（主力，受保护）/ X2 系 19.6 万（补充）/ Lite 无限量（长期后盾）/ Pro·Pro128K·Ultra 各 20w/年（未确认剩余）

**验证**：node --check；线上 pool:xf-spark-x2-flash 200；wrangler deploy（0d838982）；push→SSH pull 到 4081133。

**额度地图（2026-08-29）**：X2-Flash 1,723,155 | X2 系合计 196,242 | Lite ∞ | Ultra/Pro/Pro-128K 各 ~20w/年

---

## 轮次 2026-08-29 · 智谱+硅基流动免费模型大扩容（commit 4ffa403）

**背景**：小徐补充①智谱免费模型清单（GLM-Z1-Flash 推理 128K、GLM-4.7 500 万 tokens、GLM-4.5-Air 1200 万 tokens、GLM-4.7-Flash、GLM-4-Flash-250414、GLM-4.6V-Flash 视觉）②硅基流动免费模型（GLM-Z1-9B-0414/GLM-4-9B-0414/Qwen2.5-7B/Qwen3-8B 已在池/Qwen3.5-4B/DeepSeek-R1-0528-Qwen3-8B，L0 档 1000 RPM/50000 TPM）。

**实测结论**（智谱 9 候选直连测）：
- ✅ glm-4.7（500 万）、glm-4.5-air（1200 万）、glm-4-flash-250414、glm-4.5-flash 全 200
- ⚠️ glm-4.7-flash 热限流 429（模型存在，照常入池自愈）
- ❌ **glm-z1-flash 推理内嵌 content 的 `<think>` 且 thinking:{type:disabled} 无效（实测）**——think 吃满 max_tokens 致 JSON 截断、流式渲染思考过程，与前端协议冲突 → 禁用，待前端 think 剥离后启用
- ❌ glm-4.6v-flash 视觉模型不入池（纯文本场景）

**实测结论**（硅基流动经 worker 转发测）：
- ✅ sf-glm-z1-9b（enable_thinking 生效，无 think 直接出 JSON）、sf-glm-4-9b（真实剧情请求六字段完整）、sf-r1-qwen3-8b、sf-qwen2.5-7b 全 200
- ❌ sf-qwen3.5-4b enable_thinking 无效（reasoning_content 吃满 max_tokens，content 空 → 前端重试循环）→ 禁用
- enable_thinking 处理扩展：sf-qwen3-8b/sf-qwen3.5-4b/sf-glm-z1-9b/sf-r1-qwen3-8b 统一强制 false

**改动（worker.js）**：
1. 智谱 +10 条目：z1-flash×2（禁用）、glm-4.7×2（tier 3，300/天）、glm-4.5-air×2（tier 3，600/天）、glm-4.7-flash×2 + glm-4-flash-250414×2（tier 7，5000/天）——双 key 并行模式延续
2. 硅基流动 +5 条目：glm-z1-9b/glm-4-9b/r1-qwen3-8b（tier 95）+ qwen2.5-7b/qwen3.5-4b（tier 96，后者禁用）
3. 智谱主力段 tier 3 现 6 条目：4.7 → 4.5-air → 4-air（额度从大到小消费）

**验证**：node --check；wrangler deploy（ca6cc331）；线上 pool: 实测：智谱 4 条目（4.7/4.5-air/4.7-flash 新 key/4-flash-250414）200，硅基 5 条目 4 通（qwen3.5-4b 禁用）；push→SSH pull 到 4ffa403。**模型池 53 启用**。

---

## 轮次 2026-08-29 · 前端 <think> 剥离 + GLM-Z1-Flash 启用（commit 2f786cd）

**背景**：智谱 GLM-Z1-Flash（免费推理模型）把思考过程内嵌 content 的 `<think>…</think>` 标签，且 `thinking:{type:disabled}` 实测无效。此前禁用；本轮给前端加通用剥离后启用。这也为未来任何 think 系模型（DeepSeek-R1 等）铺路。

**改动（index.html）**：
1. 全局 `stripThink(text)`：正则 `/<think(?:ing)?[^>]*>[\s\S]*?(?:<\/think(?:ing)?>|$)/gi`——闭合块剥离、未闭合剥到末尾（流式截断场景）、`<thinking>` 变体兼容、大小写不敏感
2. 接入 10 处：
   - 协议服务入口 3 处（isStructured / parseFull / parseStreaming）——所有调用点自动受益
   - fill 协议 parse（stripFence 前剥离）
   - 流式渲染 renderStreamingFrame（normalize 前剥离；纯文本兜底渲染用剥离后文本）
   - 终版 finishStreaming（normalize 前 + 纯文本兜底）
   - 兜底布局 finalizeCardLayout（fallback story 用剥离后文本）
   - 历史清洗 buildEventRounds（判断前剥离）
3. 语义：未闭合 think 剥到末尾=宁可空也绝不泄漏思考文本；真实流式由"累积文本在 think 闭合后重新剥离"自然恢复（P12 验证）

**改动（worker.js）**：zp2/zp-glm-z1-flash 双 key enabled:false → true（tier 2，dailyCap 1500，免费推理补 X2 系额度缺口）

**验证**：extract_scripts.py 抽取 JS node --check OK；主探针新增 P11（闭合 think + JSON 正常渲染无思考文本）/P11b（未闭合全剥不泄漏）/P12（think 流式分片：think 期间无源码、闭合后骨架打字机定型、终版无 think）全过，16 用例 ALL PASS；回归 9 组全绿；wrangler deploy（771f9b8e）；push→SSH pull 到 2f786cd。

---

## 轮次 2026-08-29 · 模型池全量实测优化 55→43 启用 + 流式降级（commit e9db652）

**背景**：小徐要求「测试所有模型的性能与回复质量，思考模型池怎么优化」。对池内 55 个启用模型逐个实测（并发 2 匹配 worker gate，pool: 后门，走线上 ai.blupure.cn）：ttfb / 总耗时 / 六字段协议合规率。**合规率仅 10/55（18%）**，三类失效模式经三轮定位：

1. **平台流式 + json_object 不兼容（content 恒空）**：讯飞 v1 系（ultra/lite/pro/pro128k）流式+jo content 恒空，去掉 jo 仍慢（60s+ 读超时）；OpenRouter 流式断流/空（与 jo 无关）；硅基流式 EMPTY（与 jo 无关）；讯飞 v2 系（x1）流式+jo 空、去 jo 正常（440 字/39.9s）。**所有平台非流式+jo 全部正常**
2. **zp 老 key（ZHIPU_KEY）全线 503/坏**：7 条全废，仅 ZHIPU_KEY2 可用
3. **glm-z1-flash ×2 think 推理 >129s 仍无正文**：前端 90s 读超时必断，流式 8192 max_tokens 也超时

**改动（worker.js）**：
1. **禁用 12 条**：zp 老 key 7 条（z1-flash×2、4.7、4.5-air、4-air、4.7-flash、4-flash-250414、4-flash）、or-glm-5.2（503 免费档限额）、or-ox-alpha（503）、or-lfm-2.5-2.6b（2.6B 质量差）、sf-qwen3-8b（流式 EMPTY）——**池 55→43 启用**
2. **STREAM_BROKEN 流式降级**（新机制）：13 模型（讯飞 v1/v2 系 5 + OpenRouter 4 + 硅基 4）流式请求强制上游非流式，读完整 text 提取 choices[0].message.content，组装单块 SSE 回吐（`data: {choices:[{delta:{content}}]}` + `data: [DONE]`），前端打字机整卡显示；`X-Convert` 诊断头带 len/parsed/out/nl/safe/safenl
3. **SSE 换行陷阱修复**：上游非流式 content 可能含真实换行（格式化 JSON），直接塞 SSE data 行会拆行破坏前端解析 → `safe = outText.replace(/\r?\n/g, " ")` 压成空格
4. **zp2 tier3 排序**：4.7 → 4-air → 4.5-air（4.5-air 流式 102s 垫底）
5. **NVIDIA 段重排**：5 个实测稳定流式前置（gpt-oss-120b / gpt-oss-20b / kimi-k3 / minimax-m3 / nemotron-super），其余波动 503 熔断自愈排后兜底

**字节级验证**：r.raw 逐字节读——4 个 `\n`（526/527/540/541）= 标准单块 `data:{…}\n\n` + `data: [DONE]\n\n`，数据行无换行拆散；iter_lines 曾见 8 行系 CF 部署传播延迟打在旧版，非代码问题。5 个转换模型实测：sf-glm-4-9b 206 字合规、xf-spark-x1 196 字合规、or-nemotron-3-super 260 字合规（上游 9 换行被压平）、or-minimax-m3 282 字出内容但 opts=4（模型自身协议差）、xf-spark-ultra 上游偶发空 content（前端重试链兜底）

**验证**：node --check OK；主探针 16 用例 ALL PASS；回归 batch3_fixes 绿；wrangler deploy（3227bc8d）；push→SSH pull 到 e9db652。

---

## 轮次 2026-08-29 · 智谱 ZHIPU_KEY 换新 + zp- 条目恢复（commit c07e474）

**背景**：小徐提供两个智谱 key（K1 `0a84a2ef19f349b384040430d80038f3.QY94taHgzKuCYbAj`、K2 `dad7477d54a54903a62cddf693d1e9d1.L4w5Uhkn2YC3J8Kh`）。此前 ZHIPU_KEY 旧 key 全线 503 导致 7 条 zp- 条目禁用。

**直连实测**（非流式+jo，6 核心模型）：
- **K1**：5/6 可用——4.5-air 203字/4-air 208字/flash-250414 210字/flash 128字 全合规；glm-4.7 200 但慢（10.2s）且 content 空（推理吃满）；glm-4.7-flash 读超时
- **K2**：4/6 可用——4.7 193字/4.5-air 215字/flash-250414 170字/flash 125字 合规；glm-4-air 429 余额不足（code 1113）；glm-4.7-flash 429 限流（code 1305）

**线上现状确认**：ZHIPU_KEY2 当前已好（zp2-glm-4.7 200/4-air 224字/4-flash 165字 全合规）→ **不动**；ZHIPU_KEY 换 K1（全面可用含 4-air）。

**改动（worker.js）**：
1. `wrangler secret put ZHIPU_KEY` = K1（Secrets 不入代码）
2. 恢复 6 条 zp- 条目 enabled:true：tier3 的 4.7/4.5-air/4-air + tier7 的 4.7-flash/4-flash-250414/4-flash
3. z1-flash ×2 保持禁用（think 推理 >120s 是模型问题，与 key 无关）

**验证**：node --check OK；wrangler deploy（bd9677c1）；线上 pool: 实测 zp- 5 条合规（4.7 30.9s/4-air 8.8s/4.5-air 12s/flash-250414 9.9s/flash 19.2s）；zp-glm-4.7-flash 上游限流 503 连续 2 次（POOL_UNAVAILABLE，熔断自愈观察，tier7 低位影响小）；zp2 对照正常。**模型池 49 启用**。K2 记录备用未启用。

---

## 轮次 2026-08-29 · 六项前端修复（commit da57262）

**背景**：小徐报告 6 个前端问题，全部根因定位并修复于 index.html（单文件）。

**改动**：
1. **深色壁纸压暗**：PhoneSettingsService.apply 内联 backgroundImage 按主题拼多背景——深色下叠加 `linear-gradient(rgba(18,15,28,.62), rgba(18,15,28,.62))` 压暗（CSS 深色规则被内联样式压住，只能拼多背景）；ThemeService.setMode 补调 PhoneSettingsService.apply() 即时刷新
2. **微信红点**：`.wx-list-avatar` 移除 overflow:hidden（红点 top:-5 right:-6 越出头像被裁），img 单独 border-radius:6px；手机壳微信图标新增总未读角标（computePhoneUnreadTotal 复用 renderWechatSessions 公式 + updatePhoneBadge，刷新点：renderWechatSessions 末尾/maybePushNpcStoryWechat push 后/openWechatChat 已读后）
3. **删日记+我的回忆**（背包保留）：删除 view-inventory"我的日记"卡片、renderDiary 函数与调用、.diary-entry/.diary-body CSS、Dock"你的回忆"按钮、DEFAULT_PHONE_APPS"查看日记"App、openPhoneDiary 弹窗、phoneAppTap diary/memories case、导出表引用；背包页面与数据层、buildEventRounds/stripHtml 保留（世界页事件列表依赖）
4. **剧情页自动滚动**：scrollToBottom 去掉 hasEventCards 事件区选择（事件卡存在时原会滚到隐藏的事件容器，剧情区永不滚），view-main 固定滚 #play-chat-area
5. **事件回顾跳转**：世界页按钮「⏵ 进入」→「⏵ 回顾」；enterEvent 重写——不再依赖 data-round（appendMessage 增量路径无打标），按 history 用户轮次索引定位该轮最后一条 AI 卡，`switchView("view-main", true)` silent 切换跳过 doSwitchView 硬滚底部，scrollIntoView block:center
6. **AI 防照抄**：buildCompressedHistory「上一轮剧情结尾」注入 400→120 字（前情卡 200/400 不动，防篡改设计保留）；getResponseRules 新增第 16 条：严禁逐字复制最近记录/上一轮剧情结尾/前情原文

**验证**：
- extract_scripts.py 抽取 5 块 874527 字符 → node --check OK
- 新探针 pv_fix_verify.py：26 项文本断言 + 11 项 node 逻辑断言（真实函数体抽取执行）全过——enterEvent 定位（idx0→第1轮最后ai/idx2→第3轮）、scrollToBottom 事件卡存在仍滚剧情区、apply 深色含渐变/浅色无、未读合计与清零
- 主探针 pv_timeout_fallback 16 用例 ALL PASS（协议链不受影响）
- 回归：pv_phone_shell 3 用例绿、pv_batch3_fixes 5 用例绿，PAGEERRORS 空
- push→SSH pull 到 da57262

---

## 轮次 2026-08-30 · 进入游玩页自动滚动剧情区（commit d98f0d3）

**需求**：小徐要求——用户进入游玩页、剧情页已有剧情卡片时，自动滚动到最底部。

**根因**：`doSwitchView` 切到 view-main 的硬滚只滚外层 `main-content`，而剧情区 `play-chat-area` 是内部独立滚动容器（状态栏/tab 固定），外层滚动够不到它，进入游玩页时剧情停留在旧位置。

**改动（index.html）**：`doSwitchView` 的 `!silent && viewId === "view-main"` 分支补滚 `play-chat-area` 到底部（空值守卫）。silent 切换（事件回顾 enterEvent 跳转）保持不滚，精确定位不受影响。

**验证**：
- extract + node --check OK
- 新探针 pv_enter_scroll.py 全过（文本断言 3 + 逻辑断言 3）：非 silent 进入双容器滚底、silent 不滚、切其它页不滚
- 六项修复回归 pv_fix_verify 37 断言仍 ALL PASS（回顾跳转 silent 语义未破坏）
- push→SSH pull 到 d98f0d3

---

## 轮次 2026-08-30 · 手机壳 iOS 桌面风重构 + 微信红点修复（commit 66bd486）

**需求**：小徐反馈手机首页难看（卡片堆叠不像手机桌面，壁纸被盖住），要求学习开源项目重构为 iOS 桌面风、保留 BGM 组件；同时修复微信红点没贴微信图标的问题。

**红点根因（双层）**：
1. 锚定错误——`.phone-app-badge` absolute `right:6px` 相对整个 `.phone-app-btn`（2 列网格下约 164px 宽），微信图标 46px 居中，红点漂在按钮右侧空白处
2. 计数/刷新缺口——person-card 聊天（`person_card_chats_v1` 独立存储）不计入未读；`sendNpcMessage`/`sendPersonCardMessage`/`openPersonCardChat` 收消息路径不刷新角标

**改动（index.html，177+/99-）**：
- 布局重构：毛玻璃玩家小组件 + BGM 小组件（两行，5 钮全保留）、4 列无底悬浮图标网格（54px，label 加深色芯片）、毛玻璃 Dock（4 钮含新增微信钮），`.phone-apps-grid`/Dock `margin-top:auto` 吸底
- 红点锚定：badge 移入 `.app-icon` 内右上角（top/right -6px），updatePhoneBadge 重构为双锚点同步（`.app-icon` + Dock `.dock-icon`），防双红点
- person 未读：新增 `personChatUnreadCount`/`personChatReadMark`（`person_card_read_v1` 已读表），经 window 桥接并入 `computePhoneUnreadTotal`；3 个消息路径补齐刷新

**验证**：
- 新探针 pv_phone_redesign.py：20 文本断言 + 4 node 逻辑断言（真实函数体执行：未读计数 3→已读清零→标记归零）ALL PASS
- extract + node --check OK（877153 字符）
- 回归：pv_timeout_fallback 16 用例 ALL PASS、pv_fix_verify 37 断言 ALL PASS（六项修复未破坏）
- push→SSH pull 到 66bd486

## 2026-08-30 第X轮:手机壳 9 项修复(commit af2f44e)

| 项 | 内容 | 验证 |
|---|---|---|
| 1 | 论坛 app 独立:forum-screen 移出 wx-screen,打开即当前剧本评论区 | pv_phone_v2 |
| 2 | 微信红点:打开微信全部已读(npcChats 全标 + personChatMarkAllRead),单锚点挂 .app-icon,数字调高 flex 居中 | pv_phone_v2 逻辑断言 |
| 3 | 聊天斜杠清洗:chatMessageHtml 还原 \n\r\t 字面转义 | pv_phone_v2 |
| 4 | Dock:底部微信→存档(view-saves),找活→地图(showFullMap,新图标) | 文本断言 |
| 5 | 商店→充值弹窗(MembershipService.openPanel) | 文本断言 |
| 6 | 应用商店 app:INSTALLABLE_APPS(theater/marriage)+AppStoreService(localStorage bitlife_installed_apps_v1)+安装到主屏网格 | pv_phone_v2 逻辑断言 9 项 |
| 7 | 长按删除:事件委托 pointerdown 550ms + NoticeModal 确认,自带应用不可删 | 文本断言 |
| 8 | 全部已读:personChatMarkAllRead 跳过纯本人消息键 | pv_phone_v2 逻辑断言 |
| 9 | 回归:pv_timeout_fallback 16 用例 + pv_fix_verify 37 断言 + node --check 全绿 | 部署 |

---

## 轮次 2026-08-30 · 剧情卡加载占位 + 面板实时更新 + location 第七字段 + 时间错乱修复（commit 940c74f）

**需求**：①剧情卡前端已渲染但文字未到达时显示"正在加载文字"提示，内容到达后替换；②剧情页顶部角色面板日期/地点随剧情实时更新；③六字段协议增加第七字段 location（地点）；④剧情卡时间总错乱、莫名跳第三年，需定义时间语义并修复。

**时间错乱根因（三层）**：
1. `fillStructuredCard` 直接渲染 AI 的 time 字符串——prompt 示例写死"第3年·5月·雨夜"，模型照抄 → 卡片时间跳第三年
2. fill 协议示例写死 2026 年日期；prompt 未告知系统当前时间
3. `extractLatestYmd`/`applyPatch` 无回退保护——AI 回忆日期（如"2005年出生"）污染 state 当前时间；状态栏"第X年"与绝对年份语义混用

**改动（index.html，134+/29-）**：
- 加载占位：新增 `.stream-loading` CSS（脉冲+三点动画）；appendMessage 流式空文本、fillStructuredFrame story 区、renderStreamingFrame 空帧三处挂占位，文字到达即替换（骨架流式已覆盖）
- 时间语义定为"系统时间"：`fillStructuredCard`/`fillStructuredFrame` 时间统一用 `timeTitle(fields)`/`Utils.nowDateStr(st)`（场景名+系统时间，AI time 仅用于同步状态不上卡）；prompt 注入当前系统时间+「禁止写死示例时间/禁止跳年份」，fill 示例日期动态化；状态栏改绝对年份显示（去掉"第X年"）
- 防回退：`TimeSyncService.syncFromAiText` 与 `applyPatch` time 应用加 `ymdNum >= curNum` 防御（回忆日期拒绝）；新增导出 `extractYmdOnly`
- 面板实时更新：`renderTop` 并入 `renderPlayStatusBar`；新增 `syncStreamingPanel`（流式文本出现"前进"绝对日期即刷新面板，仅预览不触发 checkTimeEvents）；`finishStreaming` 同步 `state.location`
- location 第七字段：FillProtocolService.parse/parseFull 提取 `location`（slice 40），prompt 说明第七字段语义，finishStreaming 同步到状态与顶部面板

**修 bug**：`fillStructuredCard` 引用 `timeTitle` 跨 IIFE 作用域（StructuredCardProtocolService 内无此绑定）→ ReferenceError → 被外层 catch 吞掉显示"⚠️ 连接失败"，导致所有结构化卡不渲染。改为 `FillProtocolService.timeTitle(fields)`（7538 return 已暴露）。此 bug 曾致 pv_timeout_fallback 全用例 cardFound=false，此前探针 stub 掩盖。

**验证**：
- 新探针 pv_story_meta.py：29 文本断言 + 16 node 逻辑断言（防回退/协议 location/卡片系统时间/流式面板）ALL PASS
- pv_timeout_fallback 17 用例 ALL PASS（time 断言更新为"场景名+系统时间且不含'第1年'"）；pv_fix_verify、pv_phone_v2 回归全绿
- extract + node --check OK（888985 字符）
- push→SSH pull 到 940c74f（服务器 grep 确认 FillProtocolService.timeTitle/syncStreamingPanel 已生效）

---

## 轮次 2026-08-30 · 时间体系改故事历（commit 475ee78）

**背景**：上一轮把卡片时间强制为现实世界时间（"2026年8月30日"），小徐反馈破坏游戏沉浸感，要求更好方案。经确认改：**故事历 + AI 剧情推进**——时间从故事开局第1年开始累积，AI 在 time 字段写故事历日期，只能前进不能回退，"三年后"等跳跃合法。

**改动（index.html，40+/21-）**：
- `Utils.nowDateStr`：日期分支显示 `第X年·X月·X日`（故事历），不再显示现实日期；无年份走时代/周数兜底不变
- `extractLatestYmd`：只认 `第X年·X月·X日` 故事历（月/日可缺省取1），现实年份（2026年）不提取——回忆/照抄不再污染时间
- `applyPatch` time：年份须 1~999 且不早于当前，被拒时整块跳过（防月/日裸写）
- 开局基准：无时间时自动初始化 第1年·1月·1日（syncFromAiText/applyPatch/syncStreamingPanel 三处兜底）
- `buildBeautyRules`：注入当前故事时间，time 语义改故事历（"三日后/三年后"换算成具体日期写入、禁现实年份、禁跳回）
- `renderPlayStatusBar`：状态栏时间改 `第X年·X月·X日`
- 卡片时间（timeTitle/骨架）经 nowDateStr 自动生效，无需改

**验证**：
- pv_story_meta.py：32 文本断言 + 16 node 逻辑断言（现实年份不提取/故事历前进接受/回忆拒绝/开局初始化/超999拒绝/AI time 不上卡）ALL PASS
- pv_timeout_fallback 17 用例 ALL PASS（time 断言改为"第1年·1月·1日"）；pv_fix_verify、pv_phone_v2 回归全绿
- extract + node --check OK（890108 字符）
- push→SSH pull 到 475ee78（服务器 grep 确认"故事历"注释已生效）

---

## 轮次 2026-08-30 · 预制剧本开局时间归一化故事历（commit 待定）

**背景**：小徐反馈"所有预制的第一张剧情卡内部时间没改"——105 个 scenarios/category_*.json 的 `data.timeline.start` 写死现实/历史公历年份（现代题材约 60 个 2026，历史题材如宫斗 1725/1172、穿越 1178/2287、科幻 2387/2056、末世 2077 等），开局初始化后显示"第2026年·7月·14日"，且 >999 被故事历拒绝导致时间永久卡死（AI 写"第2027年"也被拒）。

**改动（index.html，20+/7-）——开局年份统一归一为故事历第1年（>999→1，1~999 原样，月日保留）**：
- `buildLocalInitPlan`（预制剧本开局主路径）：`currentYear: Number.isFinite(tlYear) ? (tlYear >= 1 && tlYear <= 999 ? Math.floor(tlYear) : 1) : null`
- `buildInitialState`（preset 注入统一入口，覆盖 AI 计划/重置/导入）：`>999→1`
- `StateService.init`（旧存档防御）：加载时 `>999→1`；显式 null→1（月保留）；JSON 缺字段 undefined 保持 null（走 timeEra 兜底）
- `sanitizePlan`（AI 初始化计划）：`sanitizeTime(…,1,9999)` 包装为超 999 归一 1（1~999 原样，NaN 语义保留——timeEra-only 剧本不受影响）
- 编辑器：默认 `year: 2026`→`year: 1`；保存 `numVal("sf-year", 2026)`→fallback 1 + 1~999 归一
- AI 初始化 prompt：字段说明+示例改故事历（`"currentYear":1`，禁写现实公历年如 2026/1101）

**不改**：105 个 JSON 文件本身不动（前端归一化持久防御，未来新增剧本/用户编辑写现实年份同样被归一）；first_scene 正文里 5 个剧本的年代文案（星际漂流记 2287/2023、时间修补匠 1912、书海无涯 1980）是剧情内容，extractLatestYmd 只认"第X年"不会提取污染。

**验证**：
- pv_story_meta.py：39 文本断言 + 36 node 逻辑断言（新增 7 文本 + 4 组逻辑：init 旧档 2026/5/null/缺字段、buildInitialState preset、buildLocalInitPlan 2026/1725/3/null、sanitizePlan 2026/1101/5/NaN）ALL PASS
- pv_timeout_fallback 17 用例、pv_fix_verify、pv_phone_v2 回归全绿
- extract + node --check OK（890939 字符）

---

## 轮次 2026-08-30 · 应用商店关闭按钮修复（commit 待定）

**背景**：小徐反馈"应用商店页面右上角关闭按钮不管用"。

**根因**：HTML 关闭按钮 `onclick="closeAppStore()"` 在全局作用域查找函数，但 `closeAppStore` 只定义在 AppStoreService IIFE 内部（20437），从未暴露为全局（对比 closeForumApp 20013 / closeWechatApp 20095 都有全局转发）→ 点击即 ReferenceError，按钮无效。

**改动（index.html，2+）**：IIFE 内 return 前补 `window.closeAppStore = closeAppStore;`（与论坛/微信关闭按钮的全局暴露模式一致）。

**验证**：
- pv_phone_v2 新增断言"closeAppStore 全局暴露"ALL PASS（node runner 补 global.window stub）
- extract + node --check OK
- 新 E2E pv_appstore_close.py（Playwright）：进入手机页→打开商店→点右上角关闭→断言 display:none + 无页面错误，3/3 PASS（未登录时 auth-overlay 登录遮罩常驻，E2E 先隐藏再测，与按钮功能无关）

---

## 轮次 2026-08-30 · 删星期几显示（commit 待定）

**背景**：小徐反馈"剧情卡日期不变星期几一直变"。澄清后确认方案：不做完整日期方案，**直接删掉星期几**。根因：星期几（weekdayIndex）不从日期推导，而是 AI 回复正文出现"周六/周一"等词就独立覆盖（extractWeekdayIndex）→ 每轮回复都变，与日期脱钩。

**改动（index.html，13-/7+）**：
- `Utils.nowDateStr`：删除 weekdays 数组 + weekName 拼接——故事历分支只输出 `第X年·X月·X日`，时代分支只输出 `时代：第X周`，兜底只留周数
- `Utils.shiftDays`：删除 weekdayIndex 推进行
- `TimeSyncService`：删除 `weekdayIndexFromDate`、`extractWeekdayIndex` 两个函数；`syncFromAiText` 删除"AI 文本周几词覆盖 weekdayIndex"块和"日期推进时推导周几"块
- 状态字段 weekdayIndex / AI 初始化计划字段保留（内部兼容，无 UI 影响）

**验证**：
- pv_story_meta 新增 6 条删周几断言（weekName/extractWeekdayIndex/weekdayIndexFromDate/文本覆盖均不存在）ALL PASS（45 文本 + 36 逻辑）
- pv_timeout_fallback（真实浏览器）17 用例 ALL PASS、PAGEERRORS 空——卡片时间"顶流的地下恋人：第1年·1月·1日"无周几实机确认
- pv_fix_verify、pv_phone_v2 回归全绿；extract + node --check OK

## 轮次 2026-08-30 · 时间体系改造：AI 日期原样显示（commit 0da4fdf）

**背景**：小徐确认最终方案——"AI 写日期 → 前端原样显示，几乎不拦错误，年份无限制不校验（第1年或雍正元年都行），重要的是一定要有几月几日（阿拉伯数字或中文汉字数字都要支持），避免 AI 照抄示例"。前置：开场卡时间已归一化（375e364）、星期几已删（693edb6）。

**改动（index.html，110+/79-）**：
- `Utils.nowDateStr`：**timeText 优先原样返回**（AI 日期原文三路写入），无原文走故事历数值兜底
- `TimeSyncService` 新增 `parseDateText` + `cnNumToInt`：阿拉伯 `(?:第?(\d{1,5})年)?\s*(\d{1,2})月\s*(\d{1,2})日`、中文 `(?:第?(?:[一-龥]{0,2})(数字词)年)?\s*(正/数字词)月\s*(初/数字词)(?:日)?`，最后一次匹配优先；CN_DIGITS 含 正:1、初X、元=1、朝代前缀 tail 提取；非法月日拒绝（1~12/1~31）
- `applyPatch`：字符串 time 原样写入 timeText + parseDateText 数值同步；对象 time（旧协议）格式化兼容；**删除 1~999 校验与防回退**
- `syncFromAiText`：正文提取最后一次绝对日期 → timeText + 数值，年份直接写入不拦；保留 checkTimeEvents
- 三路 timeText 写入：开局 buildInitialState/buildLocalInitPlan、协议 applyPatch、正文 syncFromAiText；init 旧存档兜底
- prompt 防照抄：timeGuide 改为格式演示+强禁令（禁止照抄提示/示例日期、禁止现实系统时间）；fill/结构化/内置规则示例 time 全部改字符串 `"5月15日"`
- 修复：mergeProtocolPatch `{...fill.time}` 展开字符串坏 → 直接透传

**验证**：
- pv_story_meta：46 文本断言 + node 逻辑（cnNumToInt 12 例、parseDateText 13 例+4 拒绝、syncFromAiText 10 例、applyPatch 7 例、nowDateStr 3 例）**ALL PASS**
- extract + node --check OK；pv_timeout_fallback 17 用例、pv_fix_verify、pv_phone_v2、pv_appstore_close 3/3 全绿

## 轮次 2026-08-30 · 禁用事件重刷按钮（commit 18b224d）

**背景**：小徐反馈重刷剧情 bug（重刷后点选项不触发生成 + 剧情区划不动滚网页），调查中发现重刷链路与滚动/生成状态关联复杂。小徐决定暂缓修复，直接禁用该功能——点击"🔄 重刷"弹窗提示"功能开发中"，rerunEvent 完整逻辑保留待后续恢复。

**改动（index.html，4+/1-）**：
- 新增 `.event-rerun-disabled` 置灰样式（opacity .45 + grayscale + cursor:not-allowed）
- 重刷按钮加 disabled class，onclick 从 `rerunEvent(${i})` 改为 `rerunEventComingSoon()`
- 新增全局函数 `rerunEventComingSoon()`：弹窗"剧情重刷功能正在开发中，敬请期待"

**验证**：
- pv_rerun_disabled 6 项全 PASS：按钮含 disabled class / opacity=0.45 / 真实点击弹窗"功能开发中" / history 未截断 / isGenerating 未置位 / 无页面错误
- extract + node --check OK

## 轮次 2026-08-30 · 支付回调对齐 h5zhifu 文档（commit 1aeb406，worker 已 deploy）

**背景**：小徐贴出 h5zhifu 回调通知接口 + 签名算法文档。核对发现回调处理两处与文档不符（都会导致"用户付了钱云币不到账"）：
1. **请求格式**：文档为 application/json，代码用 URLSearchParams 表单解析 → JSON 回调验签必败，永远 fail
2. **trade_status**：回调参数表无此字段，代码强制校验 → 若回调不带该字段永远 fail

**改动（worker.js，10+/3-）**：
- notify 路由：先 `JSON.parse`（仅接受普通对象），失败回退表单解析（兼容易支付旧版）
- `handlePayNotify`：trade_status 改为"有则校验、缺省放行"（能收到回调即已支付）
- 签名算法核对与文档一致（ASCII 升序/空值排除/&key= 拼接/MD5 大写/原始值不转义/扩展字段参与），无需改

**验证**：
- sim-worker.mjs 新增 T28-T31：JSON 回调无 trade_status → success+首充双倍 12500 到账 / 表单回调兼容 / 重复回调幂等不重复发币 / 坏签名+金额不符+trade_status=closed 均 fail 不到账——**30/30 PASS**
- node --check worker.js OK
- npx wrangler deploy 完成（my-openai-proxy，Version 0212e808）

## 2026-08-31 01:35 虎皮椒微信支付接入(08ffd8a)

- 需求:小徐已申请虎皮椒"微信支付"渠道(appid=201906186933,云吞吞数字故事平台,签约流程:管理员审核→渠道官方审核→待商户签约→签约成功),要求先改好把微信支付切到虎皮椒
- worker.js:
  - 新增 xunhuBuildSign/verifyXunhuSign(非空参数除 hash 升序拼接+APPSECRET,MD5 小写)
  - 新增 xunhuPlaceOrder(POST form 到 api.xunhupay.com/payment/do.html,XUNHU_API_URL 可配置含备用平台;返回 url 手机跳转 + url_qrcode PC 二维码)
  - createPayOrder:wxpay 优先虎皮椒(有 XUNHU_APP_SECRET 时),失败自动回退 h5zhifu
  - 发货逻辑提取为 settlePaidOrder(查单/幂等/金额分校验/首充双倍/会员),h5zhifu 与虎皮椒共用
  - 新增 handleXunhuNotify(status=OD 才发货,CD/RD 等确认收到但不发货)+ /api/xunhu/notify 路由
  - /api/pay/create 返回结构补 qrUrl
- index.html:renderOrder 支持 qrUrl 分支(PC 显示二维码图片+5 分钟提示),startPay 接受 jumpUrl/qrUrl 二选一
- wrangler.toml:注释补 XUNHU_APP_ID/XUNHU_APP_SECRET/XUNHU_NOTIFY_URL(密钥明天填 Cloudflare Secrets)
- 验证:sim-worker T32-T36 新增 5 用例 35/35 PASS(下单参数/回调发货/坏签名金额拒/退款不发货/失败回退),pv_xunhu_front 前端 3/3(二维码渲染/跳转链接/无报错),回归 pv_timeout_fallback+pv_fix_verify+pv_story_meta 全绿,node --check OK
- 部署:commit 08ffd8a push + wrangler deploy(Version a005c9e8)+ SSH pull
- 待办:①明天 appsecret 发来后填 Cloudflare Secrets(XUNHU_APP_SECRET) ②签约成功后确认 type(微信内 JSAPI 需额外绑定) ③小徐实付测试

## 2026-08-31 支付合规三项(回调原文留档+余额快照+18岁确认勾选)(f24d881)

- 需求:小徐担心用户恶意举报后"有理说不清",选了三项合规优化(服务器在香港不备案,但留存证据链)
- worker.js(+64/-8):
  - **logNotify**:每次回调(含验签失败)POST 到新集合 pay_notify_log{gateway,order_no,raw(原文截 8000),sign_valid,status,amount,trade_no},try/catch 忽略失败
  - **settlePaidOrder 快照**:发货后写 coin_ledger{user_id,order_no,delta,before,after,reason};lifetime 会员 delta=0 reason=lifetime_membership;云币 delta=实发数 reason=首充双倍到账/充值到账
  - handlePayNotify/handleXunhuNotify 均先记日志再处理(伪造/失败回调也留证);notify 路由透传原始 body 给 handler
- index.html(+10):支付面板加 18 岁/监护人同意+虚拟商品条款 checkbox(adult-confirm-box),startPay 未勾选→弹窗"请先确认"拦截不下单
- PB 新集合(scripts/pb_add_notify_ledger.mjs 已跑):pay_notify_log(admin-only,order_no 索引)、coin_ledger(user_id 索引)
- 验证:sim-worker T37-T39(留档/快照/非首充快照/验签失败也留档)38/38 PASS;pv_adult_confirm 4/4(未勾选拦截 fetch=0/勾选下单/条款文案/无报错);pv_xunhu_front 3/3 回归(补勾选后 QR/jump 均正常);回归全绿;node --check OK
- 部署:commit f24d881 push + wrangler deploy(Version 5ce947fe)+ SSH pull

## 2026-08-31 修复:论坛评论区发评论报"作品不存在"(83b5d96, worker Version f9a80862)

- 复现:游玩页手机壳论坛按钮→当前剧本评论区→点发送→弹"作品不存在"
- 根因:论坛 card_id=当前游玩剧本(官方剧本卡是前端本地 scenarios 数据),worker POST /api/game/reviews 硬校验 community_cards 集合存在性→官方卡查不到→404 CARD_NOT_FOUND
- 修复:去掉 community_cards 存在性硬校验(论坛=当前剧本评论区,官方/社区卡均可评;评论仍有登录+5秒限频+200字限制,GET 列表本就按 card_id 过滤不校验卡存在)
- 验证:sim-worker 新增 T40-T43(未登录401/社区卡评论成功+列表可取/官方卡评论不拦截/缺参数400)**42/42 PASS**
- 部署:commit 83b5d96 push + wrangler deploy(f9a80862);index.html 未改无需 SSH pull

## 2026-08-31 屏蔽词过滤(社区发帖/评论三级拦截)(28f0512, worker Version 25386511)

- 需求:小徐提供 tomzhang/bannedwords 现成词库,给社区动态+评论区加基本屏蔽词规则
- 词库:banned_words.js(新文件,自动生成)——合并 pub_banned_words(1673 条)+ pub_sms_banned_words(2696 条)base64 解码去重,共 3949 条/58KB,随 worker 部署
- worker.js:checkBanned(text) 子串匹配返回首个命中词;纯字母/数字词加前后边界(避免 obsessed 含 sb 误伤)
- 挂载:发帖 POST /api/posts、动态评论 POST /api/posts/comments、讨论区评论 POST /api/game/reviews → 命中 400 BANNED_WORD,错误信息含具体命中词
- 验证:sim-worker 新增 T44-T48(发帖/动态评论/讨论区评论违禁拦截不落库/正常发帖/字母词边界)47/47 PASS
- 部署:commit 28f0512 push + wrangler deploy(25386511);index.html 未改无需 SSH pull

## 2026-08-31 AI 内容策略调整:用户输入打码、AI 输出放宽(ae4f993)

- 需求调整:小徐指示 AI 生成内容过滤不要太严格(避免误伤剧情质量),重点规范用户剧情页发给 AI 的内容
- index.html:
  - 内联词库块(3949 条,filterBannedText 等长*替换+首字索引+字母词边界,与 worker 同源)
  - 用户输入三入口发送前打码:sendUserText(剧情页)/sendNpcMessage/NPC 聊天、sendPersonCardMessage(人物卡聊天)→ 显示/存储/发给 AI 均为打码后文本
  - 撤销 AI 输出三出口(onToken/streamCompletion/requestCompletion)与 fillCardTemplate/fillStructuredCard 字段打码(28f0512 的 worker 端发帖/评论拦截保留,与 AI 无关)
- 验证:pv_banned_filter 探针 17 项全 PASS(替换/边界不误伤/JSON 结构安全/性能 7ms/出口负断言/三输入入口正断言)+ pv_timeout_fallback 流式回归 ALL PASS + node --check OK
- 部署:commit ae4f993 push + SSH pull

## 轮次记录 2026-08-31 SEO/安全
- 修复:nginx deny /docs/(部署清单等敏感文件曾可公开访问,curl 200→403),bitlife.conf 加 location ^~ /docs/ { deny all; }(备份 .bak.20260831),nginx -t + reload 通过
- SEO:接入 IndexNow(key f3f7344a48003610e54a75a116e183f4,key 文件已部署,API 提交首页返回 202)+ sitemap 加 lastmod 2026-08-31 + robots.txt 加 Disallow: /docs/
- 验证:docs 403/首页 200/sitemap 200/key 文件 200 内容正确
- 部署:commit 947f567 push + SSH pull

## 轮次记录 2026-08-31 支付路由重构
- 路由:电脑端微信=虎皮椒扫码(qrUrl,失败回退 h5zhifu)；手机端微信/支付宝=h5zhifu(H5 收银台跳转)；电脑端支付宝=报错"请在手机端登录网站使用支付宝"(H5 收银台被支付宝 UA 拦截)
- 前端:selectedPayType 按 UA 默认(电脑=wxpay/手机=alipay),startPay 电脑端支付宝拦截提示,renderOrder 手机端不显示二维码(上一轮 d7ade5a)
- worker:createPayOrder 加 isMobile 参数,h5PlaceOrder 独立函数,路由按 UA 判定
- 验证:node --check + pv_pay_route_probe/pv_worker_route_probe ALL PASS + 线上 grep
- 部署:worker Version a0161cf0(wrangler deploy) + commit 999f13a push + SSH pull

## 轮次记录 2026-08-31 支付收尾
- c6 档测试价 0.01 元已恢复 6 元(worker f1f96ffd + commit dc86335)
- 支付宝确认未开通(前后端拦截),提示含小红书官方账号「云吞吞在努力」联系渠道(worker 78aa3621 + a7bf11e)
- 支付路由最终形态:电脑微信=虎皮椒扫码(失败回退 h5zhifu)/手机微信=h5zhifu/支付宝=未开通
- 修复:支付成功弹窗文案用服务器 plan_id 判断(worker b99dcfec);nginx index.html 加 Cache-Control no-cache 防旧缓存

## 轮次记录 2026-08-31 开场卡身份修复
- Bug:冷宫惊梦(gd_01)主角默认身份显示"皇帝"——identity.role 缺失时 buildLocalInitPlan 从背景文本 ROLE_DICT 匹配,"皇帝震怒"误配;根因是 scenario-schema.mjs 白名单丢弃 role 字段(数据有也会被删)
- 修复 3 层:
  1. scenario-schema.mjs:identity 白名单加 role(20 字),text 拼装加"身份:XX"行(AI 可读)
  2. 56 张卡补齐 identity.role(娴妃/天文学家/医生/外卖骑手/绣娘/锦衣卫/书吏/铁匠/女帝/咒术师等;脚本批量注入避免手误)
  3. index.html buildLocalInitPlan 推断加固:封X妃句式→职业词兜底(先剔除父亲/母亲/恋人等他人句)→称谓词(皇帝/王爷/公主/太子/皇子/妃嫔)不参与兜底
- 顺带修:gd_02 凤临长安 timeline 712→713(开元元年)
- 验证:全量 115 卡扫描 role 56+句式 1+兜底 28(全部人工核对比正确)+theme 17(可接受)+空 13(无职业);pv_role_probe ALL PASS;线上 grep roleFromBg/娴妃 命中
- 部署:commit 84749ff push + SSH pull + IndexNow 提交

## 轮次记录 2026-08-31 评论弹窗修复
- Bug:社区动态点击"发送"评论成功后弹窗不消失——sendPostComment 成功后调用 openPostComments 重开弹窗(display:flex)
- 修复:成功后改为 closeCommentsModal() + loadCommunityFeed()(内联评论与计数随之刷新);失败保持弹窗并提示
- 验证:index.html:19216 线上 grep openPostComments(评论按钮)+closeCommentsModal(成功分支)
- 部署:commit cabc2b8 push + SSH pull + IndexNow(api.indexnow.org 官方端点,本地直连 blupure.cn 证书校验失败)

## 轮次记录 2026-08-31 全忙判定 5s 预算
- 需求:任何场景下"模型池全忙判定"不超过 5 秒(原最坏:全故障 15s×49≈12 分钟/全并发满 8s×49≈6.5 分钟,前端 90s 读超时兜底)
- 改动(worker.js):
  1. POOL_JUDGE_TIMEOUT_MS=5000 全局硬预算,候选遍历循环顶检查,耗尽立即 503
  2. gateAcquire 加 maxWaitMs 参数(默认仍 acquireTimeoutMs 8s),循环内传剩余预算
  3. fetch 超时 = min(默认 15s/120s, 剩余预算),流式 body 透传不受影响
  4. 429 Retry-After 等待 cap 到剩余预算
  5. setModelCooldown 改同步内存 + Cache API fire-and-forget(防 Cache 网络延迟吃预算)
- 权衡:响应头 >5s 的慢模型(OpenRouter/NVIDIA 兜底位)不再被等,正常路径(tier1-4 通常 <2s)不受影响
- 验证:node --check + pv_pool_budget 5/5 PASS(全故障 5006ms/全并发满 5008ms/全熔断 0ms/正常 496ms/混合 5012ms) + 线上 pool:xf-spark-lite 200 4.86s
- 部署:wrangler Version 908c951f;worker.js 在 .gitignore 不进 git

## 轮次记录 2026-08-31 清理缓存后强制刷新
- 需求:我的页面"清除本地缓存"完成后强制刷新浏览器
- 改动(index.html clearLocalCache):清理成功 → showInfo 提示"页面即将刷新" → 1.5s 后 location.reload()(去掉无意义的 renderAll)
- 验证:逻辑简单,nginx no-cache 已配,reload 即取新页面
- 部署:commit 242e9e8 push + SSH pull + IndexNow 202

## 轮次记录 2026-08-31 5s 预算回滚:慢模型不截断
- 需求:去掉全忙判定 5s 硬预算——兜底位慢模型(OpenRouter/NVIDIA 响应头 8-26s)必须完整等待,不能被截断
- 权衡说明:完整超时(15s/120s)×49 候选串行 与 快速判定数学上不可兼得;依靠熔断自愈(首轮失败即熔断,后续请求 <1s 跳过)+ 前端 90s 读超时兜底
- 改动(worker.js):删除 POOL_JUDGE_TIMEOUT_MS/循环预算检查/gateAcquire maxWaitMs/fetch 超时 cap/429 sleep cap;保留 setModelCooldown 内存同步+Cache 异步广播(与预算无关的熔断提速)
- 验证:node --check + pv_slow_model 4/4 PASS(前39失败第40慢模型8s成功命中/正常路径0.3s/15s完整超时后下一候选成功/全挂快速失败3s)
- 部署:wrangler Version 8fe59219

## 轮次记录 2026-08-31 流式坏模型针对性修复
- 需求:修好 13 个"流式+JSON 模式"坏模型,最小改动
- 搜索结论:SiliconFlow 官方支持流式+json_object(需拼分片);GLM 思考模型 JSON 被路由到 reasoning_content(sglang #22042);OpenRouter 发 SSE 注释块/空块需跳过(openai-go #621);gpt-oss-20b:free 设 max_tokens 即空响应;prompt 含 "json" 字样可缓解
- 实测(线上 pool: 后门,流式+剥 response_format):讯飞 5 个(x1/ultra/lite/pro/pro128k)恢复流式出内容(x1 裸 JSON,其余带 ```json 围栏前端 stripFence 容错);OpenRouter 4+硅基 4 流式仍零输出(chunks=0)只能非流式
- 定稿(worker.js):STREAM_NO_JSON 新集合(讯飞 5,流式+剥 format);STREAM_BROKEN 缩为 8 个(保持强制非流式单块回吐);upstreamNonStream 变量记录上游实际是否流式,回吐分支按它判断
- 验证:复测 13/13 出内容(讯飞多 chunk 真流式/其余单块非流式);前端 parseStreaming 按行解析兼容讯飞换行编码;node --check
- 部署:wrangler Version cddf12ed

## 轮次记录 2026-08-31 GLM-5.3-Flash 终极兜底上线
- 需求:兜底模型用原智谱 API,仅当所有免费模型失败/熔断/限流时使用付费模型 GLM-5.3-Flash
- 改动(worker.js):MODEL_POOL 尾部新增 { id: zp-glm-5.3-flash, apiKeyEnv: ZHIPU_KEY3, model: glm-5.3-flash, dailyCap: Infinity, tier: 100 };pickModel 优先 tier1-99,候选链排序也在最后 → 天然只在免费链全挂后命中
- 排查过程:首版 apiKeyEnv=ZHIPU_KEY2 线上 503 POOL_UNAVAILABLE;直测智谱(open.bigmodel.cn/api/paas/v4/chat/completions):模型名有效(glm-5.3-flash 在 models 列表),F:\Claude\zhipu-apikey 的 key 调付费模型 HTTP 200 且输出正确 JSON → 判定 ZHIPU_KEY2 无付费权限
- 定稿:新增 secret ZHIPU_KEY3(值=zhipu-apikey 文件已验证 key),条目 apiKeyEnv 改 ZHIPU_KEY3
- 验证:pool: 后门流式 HTTP 200, x-model-used=zp-glm-5.3-flash, x-model-attempts=zp-glm-5.3-flash:200:806ms, len=63 jsonValid=true;非流式也 200
- 部署:wrangler Version 7aba950e

## 轮次记录 2026-08-31 充值新增 1 元档
- 需求:充值额度/挡位改小,至少支持买 1 元(终身会员除外)
- 改动(worker.js CHARGE_PLANS + index.html AppConfig.chargePlans):新增 c1 ¥1 = 1000 云币(+100 赠送,首充双倍 2000);前端展示卡、默认选中改 c1(首位卡高亮+防误触大额);worker 校验/发货/首充判定逻辑天然复用,无其他硬编码
- 验证:pool 无关;直测 /api/pay/create:c1 → 200 订单 MPMTHB2HV19X2SD9 带支付链接+二维码;c6 回归 200;c999 → 400 正确拒绝
- 部署:worker Version 610b2ab7;前端 commit 8bd16bd + push + SSH pull + IndexNow 202

## 轮次记录 2026-09-02 会员改革+免费用户额度上线
- 需求:会员改革——周/月/季/年会员档(AI 对话不限次不耗云币),免费用户每日免费 AI 额度,云币回归"解锁剧本卡+额度超限后 AI 对话"
- 改动(worker.js + index.html, commit dce14f1):
  - MEMBER_PLANS: weekly ¥9/7天、monthly ¥21/30天、quarterly ¥49/90天、yearly ¥118/365天;下单校验/回调发货/续费不吞时长(max(现到期,现在)+时长)/coin_ledger membership_{id} 全通;lifetime ¥188 保留
  - 免费额度: FREE_QUOTA_PER_DAY=50,北京时间 08:00 刷新(KV key freequota:{uid}:{date},48h TTL,非原子少计可接受);freeMode 仅路由 NVIDIA 全部+sf-glm-4-9b、成功后计数、不扣币;freeExhausted 自动转云币模式(全池,请求前最低余额校验,不足 402 INSUFFICIENT_COIN);会员过期自动回退免费额度(靠 expires_at 判定,membership_type 保留)
  - 响应头 X-Free-Exhausted=1 标记本次已转计费;前端每会话仅提示一次(sessionStorage)
  - /api/usage 新增 memberDaysLeft/freeQuotaExhausted(数量不展示给用户)
- 验证:本地 freequota_probe.mjs 40/40(mock PB+AI:首调不扣币/49→50 边界/第51次转云币扣费/会员不限次/过期回退/402/流式结算/usage 字段/下单 weekly monthly yearly c1 200+无效档 400);生产冒烟 13/17:usage 新字段 4/4 通过、免费 chat 第二次 200 无耗尽头(首次 503 为 NVIDIA 上游瞬时故障,非代码问题,同链路第二调即恢复)
- 部署:worker Version 09f71a6e(2026-09-02);前端 commit dce14f1 push + SSH pull + IndexNow
- 备忘:免费模式无跨平台 fallback(NVIDIA+sf-glm-4-9b 全挂时免费用户 503「请稍后重试」为预期设计);新探针在 scripts/freequota_probe.mjs(本地,gitignore 不入库)

---

## 轮次记录 2026-09-02 · 创作向导表单残留修复 + 免费用户并发排队

### 1. 创作向导 Bug 修复(commit 4203bb3)
- 现象:三步向导确认生成后,编辑器显示上一张卡(圣罗兰学院)的表单,新卡已保存但界面不刷新
- 根因:wizardConfirm 保存成功后缺 SettingsService.loadToUI()/renderAll()(正常选卡路径有);草稿恢复分支同样缺 → 表单 DOM 停留进向导前渲染的卡
- 修复:index.html 两处补齐 leavePlay/init/loadToUI/renderAll/switchView 顺序;saveCards 加 try/catch(存储满提示并停留向导,绝不带错误切走)

### 2. 免费用户并发排队(worker Version 83903ffd + commit 17d60b9)
- 问题:免费池(NVIDIA+sf-glm-4-9b)并发/限流被占满时,每候选 gateAcquire 空等 8s,最坏 16 候选 ~128s 后 503,用户无排队预期
- worker:
  - gateAcquire 支持 timeoutMs 参数(默认不变);免费模式候选判定 400ms,全忙最坏 ~6.4s
  - 免费模式全候选不可用 → 立即 429 QUEUE_BUSY + body detail.queueLen + X-Queue-Len + Retry-After:3(失败不扣币不计数);云币/会员保持 503
  - queueLen=本实例 Map<userId,lastSeen≤10s> 去重活跃排队数,最小 1;CF 多实例为近似值(规模小可接受)
- 前端(剧情输入框上方黄条):
  - workerCode QUEUE_BUSY 识别;fetchWithRetry 对 QUEUE_BUSY 直通(不走 3 次快重试)
  - queue-bar:"⚠ 模型繁忙,正在排队·前方 X 人(已等待 Ns)" + 「开通会员免排队 >」入口(MembershipService.openPanel)+ ×关闭(取消本轮排队)
  - callWithQueueRetry:3s±400ms 抖动轮询重发同一请求,上限 60s;成功隐藏黄条正常出剧情;超时黄条转红"当前模型繁忙,请稍后再试";其它错误(503/402)走原路径
- 验证:freequota_probe.mjs 54/54(原 40 + 排队 14:全挂免费 429/云币 503/不扣币不计数/并发打满 400ms 快判定 u2 429/queueLen≥1/溢出请求 429)
- 部署:worker 83903ffd + commit 17d60b9 push + SSH pull + IndexNow 200
- 备忘:429 排队轮询不扣免费额度(bump 仅成功路径);结构化重试等内层请求同样走排队,一轮最多再等 60s;排队黄条文案含会员引导

---

## 轮次记录 2026-09-02 · 创建订单卡顿止损(worker 8f6d0570)
- 需求:手机端/电脑端创建订单巨卡——实测手机端每次下单稳定白等 ≥15s
- 根因:h5zhifu 网关首节点 open.h5zhifu.com TLS 失联(2026-09-02 本地直连与海外代理出口均 CODE 000),PAY_GATEWAY_BASES 串行从首节点 15s 超时才切备用 → 每单 ≥15s;前端 fetch 20s abort 后 worker 仍会下单成功 → pending 孤儿堆积(线上 56 单仅 1 paid)
- 实测:bj/sh/gz/cd.open.serverapi.work 全部 0.15-0.2s 健康;虎皮椒 api.xunhupay.com 0.2s 健康
- 修复(worker.js):移除已死首节点(注释留存,平台主域恢复需连通性验证再回添);健康备节点置首位;h5 网关超时 15s→4s,虎皮椒 15s→8s 快失败切换
- 验证:node --check + 线上探针 /api/pay/create 无 token → NOT_LOGGED_IN;/api/pay/offer → 401
- 部署:worker Version 8f6d0570(2026-09-02);前端无改动
- 备忘:待小徐实付自测(手机微信收银台/PC 虎皮椒扫码 ¥1)确认可打开且回调到账;56 条 pending 孤儿单留存观察,清理需另行确认

---

## 轮次记录 2026-09-05 · 防克隆加固(worker 6f2e747c + commit d5164de/db50266 + PB drop-in)
- 需求:云吞吞 H5 被第三方打包成 APK 散布下载站,填入任意 OpenAI 兼容 API key 即全功能可用(纯前端引擎+BYOK 可插拔端点,克隆零门槛)。定性:非服务器入侵/数据泄露,是公开 H5 被搬运+官方 BYOK 特性被利用。PRD:docs/prds/bitlife-anti-clone-prd.md(工作区)
- 方案 A worker 来源闸门(worker.js):
  - isAllowedOrigin:白名单 = blupure.cn 子域 + localhost/127.0.0.1;入口 fetch 第一道校验,带 Origin 非白名单 → 403 FORBIDDEN_ORIGIN
  - 无 Origin(同源 GET/支付回调/curl)放行,业务安全由 X-Auth-Token + 回调验签承担
  - 单测 origin_guard 19/19(含 evilblupure.cn 子域混淆/pages.dev/github.io/Origin:"null" 全拒);线上 8/8(官方 200、伪造 403、notify 无 Origin 放行走验签 fail、byok/check 401/403)
- 方案 B BYOK 登录门槛(index.html + worker.js):
  - 前端 saveByokFromUI 改 async:三项填齐后先 POST /api/byok/check(带 X-Auth-Token),未登录/会话失效不写 localStorage + 弹「请先登录」引导;loadByokInputs 抽函数复用
  - worker 新增 POST /api/byok/check:authenticate 登录校验,通过返回 {ok:true}
  - 竞态修复:saveUserApiFromUI 改 async await(原同步 loadToUI 回填会覆盖异步校验期间的输入)
  - 浏览器实测 3/3(游客填三项→localStorage bitlife_byok_v1 仍 null+弹窗「自备 AI 接口需登录官方账号」)
  - 局限:BYOK 直连链路服务端无法强制,门槛挡打包党开箱即用,铁心逆向者防不住(接受)
- 方案 C PB CORS 收口(服务器 47.238.246.167):
  - pocketbase.service drop-in /etc/systemd/system/pocketbase.service.d/cors-restrict.conf:ExecStart --origins="*" → --origins="https://bitlife.blupure.cn,https://db.blupure.cn"(回滚=删 drop-in+daemon-reload+restart)
  - 探针 4/4:官方 Origin ACAO 回显、伪造 Origin 无 ACAO 头(浏览器拦截)、无 Origin 正常、管理台域名正常
- 部署:worker Version 6f2e747c(2026-09-05);前端 commit d5164de(P1)+db50266(竞态)push + SSH pull;PB drop-in 重启(秒级,Restart=always)
- 备忘:¥1 实付自测已取消(小徐 2026-09-05 确认不做);克隆源投诉下架暂缓;观察期 1-2 周盯微信内置浏览器 Origin 行为;后续可选迭代=玩法服务端化/套壳 UA 识别/克隆源月度巡检;旧 worker CORS 仍为 *(被闸门挡在业务前,不影响)

## 轮次记录 2026-09-05 · 免费额度 50→10 次/天 + 下架每日签到(worker bb18a9c5 + commit cb98cb6)
- 需求:小徐拍板——每人每天免费 AI 额度 50→10 次调用;同时删除签到领云币功能(决策:充值+会员双引导文案/前端不展示剩余次数/立即生效)
- worker.js(已 wrangler deploy,Version bb18a9c5):
  - FREE_QUOTA_PER_DAY 50→10(北京时间 08:00 刷新不变,额度内仍限 NVIDIA 池+sf-glm-4-9b,超限转云币计费)
  - 删除 /api/checkin 路由(发币+写 last_checkin_date/checkin_streak 的整段逻辑);删 CHECKIN_BASE/CHECKIN_STREAK_BONUS/CHECKIN_FIRST_BONUS 常量;usage 响应删 checkinStreak/checkinDate 字段;经济规则注释去签到描述
  - 线上验证:POST /api/checkin → 404;本地回归 freequota_probe 57/57(适配 10 次边界:第 10 次仍免费/第 11 次转云币计费/usage 无 checkin 字段/checkin 404 等)
- index.html(commit cb98cb6,push + SSH pull 已生效):
  - 会员面板删「每日签到」按钮+签到状态 msg 行;renderCoinBalance 只留余额;删 doCheckin/applyCheckin 及 export;cacheUser/restoreCachedUser 去 checkin 字段
  - 两处 INSUFFICIENT_COIN 文案改充值+会员双引导(「云币不足，请充值或开通会员后继续（开通会员可 AI 不限次）」+ 引导路径「我的」→ 充值/云币 或开通会员);解锁弹窗文案去签到提法
- 备注:PB 用户记录存量 last_checkin_date/checkin_streak 字段保留无害(无任何读写方);线上 URL 静态检查已确认无签到残留(本次按小徐指示只做代码检查,未做浏览器实测)

## 轮次记录 2026-09-05 · 社区预制动态 50 条(worker a5610241 + seed 数据入 PB)
- 需求:小徐拍板——社区动态页预置 50 条「很像真人」的动态,由不同用户发出(头像/昵称尽量不同),部分带配图,永久保留不被 30 天清理删掉(决策:头像用真实头像库图片;只豁免删除、接受沉底不调 feed 排序;少量互动+配图)
- schema(scripts/pb_setup_collections.mjs 同步):posts 集合新增 seed text 字段(≤16),seed 非空=预制动态;脚本已幂等补齐线上
- worker.js(已 deploy,Version a5610241):scheduled 30 天清理 filter 由 `created<'cutoff'` 改为 `created<'cutoff' && seed=''` —— 预制动态永久豁免,用户发的正常动态照常 30 天清理
- 种子数据(PB admin 直写,一次性脚本 scripts/pb_seed_community.mjs,幂等:已有 seed='v1' 即跳过):
  - 50 个种子用户 seed_u01~u50@seed.yuntuntun.local(昵称各异真人向,faceimg=https://bitlife.blupure.cn/avatars/m0-25/w0-23.jpg)
  - 50 条动态 seed='v1',12 条带场景封面配图(https://bitlife.blupure.cn/scenarios/covers/…);101 条跨用户点赞(1-3/帖)、28 条跨用户评论,likes_count/comments_count 与实际记录一致,无自赞自评
  - created/created_at 由 SQLite 直改回填分散到 2026-08-16 ~ 2026-09-04(过去 1-20 天,天然沉底在用户新帖之后)
- 头像库:56 张真人头像(randomuser.me portraits)服务器直下到 /var/www/bitlife/avatars/(目录原有 avatar_*.jpg 44 张未动);外网 4 个 URL(头像/封面)实测 200
- 校验:自赞 0/自评 0(曾 1 条自评已 SQL 修正)、counts 全对齐;feed 接口需登录(worker authenticate),数据层经 PB admin 直查校验完整
- 备注:种子用户邮箱域 seed.yuntuntun.local 不可收信,仅作记录主键;密码随机 12 位无记录(不可登录,无需登录);防克隆 Origin 闸门对该功能无影响

## 轮次记录 2026-09-05 · 社区动态页空白修复——feed N+1 撞 CF 子请求上限(worker 2c906aa1/3b48e4e0)
- 症状:小徐刷社区页看到「还没有动态,来发第一条吧」;数据层 51 条完好(PB 直查),feed 带登录 token 逐帖 join 在数据只有 1 条时不炸,seed 50 条后必现
- 根因:worker GET /api/feed 逐帖串行 join(作者+关注+点赞+评论+评论作者,5-6 请求/帖),perPage=20 即 100+ 子请求;CF Worker 单次调用子请求上限 50 → 超限 500「Too many subrequests」→ 前端把 500 body 当空 items 渲染空态文案(前端不检查 res.ok)
- 修复(worker.js):
  - GET /api/feed 改批量 join:posts 1 次查询 → 用户 id OR 链批量拉(60 块防 URL 超长)、评论按 post_id OR 一次拉回应用层取前 2、点赞/关注关系各 1 次批量过滤;固定 ~6 子请求,perPage 50 稳定 ~1s/90KB
  - 评论作者增量补齐(loadUsers 去门闩,已拉 id 去重)
  - GET /api/posts/comments 同改批量(原每条评论 1 次用户查询,100 条评论即超限)
  - getAdminToken 加 invocation 内缓存(原每次 pbAdminFetch 都重新登录 PB,浪费一半请求)
- 验证:feed perPage 20/30/40/45/50 全 200(50 条 3 次复测稳定,~1s/90KB,28 条评论作者资料 0 缺失);comments GET 200 资料齐全;部署灰度期曾残留旧版节点 500,传播完稳定
- 备注:GET /api/game/reviews(recent=1 每评论 2 请求/card_id 每 20-50 评论作者请求)与 /api/characters/favorites(每收藏 1 卡请求)仍为 N+1,数据量小暂未触发,遗留观察,量大后再批量改造

## 2026-09-05 语言文游 M1 + SEO 修复
- commit 35546e3:语言文游 M1——首页入口横幅+语言大厅(语种/测档/沉浸,本地档案 lang_profile_v1)
- commit a20f3fe:必应 SEO——title 加长、8 处 img 补 alt

## 2026-09-05 语言文游 M2(追加)
- commit cb0f567:worker lang API(GET|PUT /api/lang/profile、GET|POST /api/lang/vocab、PUT /api/lang/vocab/status)+PB lang_profiles/lang_vocab 集合;大厅测档扩为 10 题由易到难(难度加权计分,≥12/19→A、≥6/19→B);band/immersion 登录态云端同步
- PB:lang_profiles/lang_vocab 已建(集合共 20,余 16 业务) | worker:cb0f567 部署同步 | 前端:cb0f567
- 小徐侧待办:手机试玩 10 题测档(改档/重测/沉浸)并验证云同步

## 2026-09-05 语言文游 M3(英语生成核心,首轮试玩验收点)
- commit 6b1d2e9:LangEngine 旁挂 play 引擎——英语会话存档 -lang 隔离/中文档零污染;systemPrompt 注入英文最高优先级指令(全英语输出+语气保真词典 10 题材+ABC 三档措辞+渐进前 2 轮〔小译〕);大厅语言剧本库(官方卡列表 EN 徽标/继续标记);点卡英语开局(AI 生成英文首轮)或续英语档;启动自动恢复英语会话,中文入口自动退出
- 试玩路径:首页语言文游横幅→语言大厅→(测档/改档)→进入语言模式剧本库→点卡即玩
- commit a2150b9:素材 4 张已生成入库(cogview-4):lang/banner-entry 1600x560 / hall-top 1280x400 / recap-top 1280x240 / vocab-head 1280x320,均 ≤150KB;URL https://bitlife.blupure.cn/lang/*.jpg(前端引用 M4-M6 就位)

## 2026-09-05 语言文游 M4(辅助层:点译/生词/生词本)
- worker Version 7a5a5057-21e1-448e-b9e9-347c446f6b6d:新增 POST /api/lang/gloss 合批点句翻译(≤10 句/批、6s/人限频、MODEL_POOL 候选链直调:zp-glm-4.7-flash→zp2-glm-4-flash→zp-glm-4-flash→zp-glm-4.5-air→nv-gpt-oss-20b→zp-glm-5.3-flash,45s 超时、JSON 协议 {items:[{idx,zh,words:[{w,en,zh}]}]} 需登录)
- commit 7371252:前端辅助层(index.html +630/-1)——①点句即译 GlossCard:剧情句点击弹整句译文+难词 chips,内存缓存+40ms 合批,渐进模式前 2 轮自动展开、第 3 轮起收起,未登录提示但不发请求;②生词标色点查 WordPopover:蓝色虚线下划线生词点看 EN 释义+中文兜底,登录后点击自动 POST 收进 lang_vocab(origin 记卡名);③⑤ 我的生词本视图:全量拉取按时间倒序,「新学→眼熟→已掌握」三级手划(PUT /api/lang/vocab/status);④素材接入 3/4:banner-entry/hall-top/vocab-head(recap-top 留给 M5 复盘)
- 待办:M5 复盘(recap 合批接口+章末复盘清单+收藏闭环),素材 recap-top 此刻接入

## 2026-09-05 语言文游 M5(章末复盘:recap 接口 + RecapModal + 收藏闭环)
- worker Version b5397670-1519-44f8-854a-16d3098028db:新增 POST /api/lang/recap(登录+6s 限频;story 剧情 ≤8000 字符 + band 档位 → 直调模型(候选链同 gloss)一次出 {expressions[≤6 条 en/zh/example], writing[≤3 句]};40s 超时;curl 探路由 401 ✓)
- commit a8358a0:前端(index.html +343)——每 5 轮一小章:AI 卡尾挂「📋 第 N 章小结」轻量入口条 → RecapModal(顶 recap-top.jpg):①本局点查词回顾(chips 即时展示,新会话自动清零)②「✨ 生成剧情高频表达+仿写例句」(渐进展示:未登录→登录引导/生成中 spinner/失败重试/6s 限频提示)③仿写例句;每条 ★ 一键收藏落 lang_vocab type=expression(服务端幂等去重,已藏 ✓),单字收藏回填正文高亮;生词本视图表达条目拼例句展示;Esc/点空白关层
- 复盘 AI 内容:剧情窗口=历史最近 ≤5 轮 AI 文本;band 措辞随测档(a/b/c)
- 待办:M6 打磨(渐进沉浸状态机联调、语气词典扩充、免费/会员额度边界检查)→ 已完成 2026-09-05(M7 打磨轮次见文末;M6a/b/c 轮次已一并补录)

## 2026-09-05 语言文游 M6b(英语卡链路:lang_cards 集合 + 只读接口 + 前端库替换)
- 需求:剧本库数据源从官方中文卡切到 PB lang_cards(英语原生卡);v2 核心转向——首发卡内容工作流独立(卡 01-05 成稿见 docs/english-cards/)
- worker Version 56d212a3:新增 POST /api/lang/cards(超管建卡)+ GET /api/lang/cards?lang=en(公开只读,按 order 排序,60s 缓存)+ POST /api/lang/play(计数)+ POST /api/lang/report(unlock 计数);lang_cards 集合(record id=管理自定义字母数字)
- PB:lang_cards 集合已建;⚠️ PB number 字段 0 值判 blank 的坑——play_count/unlock_count/order 必须 required:false(setup 脚本 scripts/pb_setup_collections.mjs 已同步),否则 0 落库失败
- 前端 commit 6f94004:window.LANG_CARDS_ONLINE/__LANG_CARDS_READY/__LANG_CARDS_LOADING 全局 + LangController 剧本库切数据源 + 语言首页(测档/沉浸/继续)+ 播放计数;词库五档(见 M6d1-d6 轮)
- 备注:语言卡 id 与官方中文卡 id(scenario_preset_*)命名空间隔离,无冲突;本段为压缩会话补录,原提交 6f94004

## 2026-09-05 语言文游 M6c(首发英语卡 01-05 成稿上架)
- 内容工作流:大纲(card-0X.md)→ 成稿(card-0X.card.json,editor 协议:全英文 text=world bible + structured{band/theme/world/identity/npcs/timeline/scene_style/first_scene})
- 卡清单:01 First Semester(cet4,校园,30 词池)/ 02(月光小馆,cet6)/ 03 The Last Train Home(cet6,都市怪谈)/ 04 The Night Watch(ky,奇幻城防)/ 05 Echoes of the Deep(toefl,科幻监听)——首轮零 AI 本地渲染(first_scene 221-321 词),词池逐词过词库核验(cet4×30/cet6×30/ky×42/toefl×38)
- 提交:434bed7(卡01+lang_cards number 字段 required:false 修复)/ 4b8d36b(卡02-05);/api/lang/cards?lang=en 5/5 返回(order 1-5)
- 备注:原提交信息带 Co-Authored-By 笔误(不影响仓库内容);PB 超管直写落库;本段为压缩会话补录

## 2026-09-05 语言文游 M6a(独立页面组,commit 49ace01)
- top-nav 插入「学语言」tab(data-view=view-lang);view-lang 重构为语言首页(品牌区+语种卡 EN active/ja-ko locked+测档 hero/四 CTA 锚点/推荐剧本行按 band match/继续学习行 hasLangSave)
- 新子页 view-lang-library:英语原生卡剧本库(六档 chips 全部/hs/cet4/cet6/ky/toefl + 题材 chips 动态 category_zh + 继续·第 N 轮角标);新子页 view-lang-learn:收纳 ②测档③沉浸⑤生词本⑥精读(原 DOM IDs 全保留,旧 JS 零迁移)
- 语言自导航 .lang-sub-nav 三按钮;播放返回链:switchView 里 view-home 且 prevView∈播放态集且 isLangSession → 改落 view-lang;语言页内点主站首页正常回中文(不困死)
- LangController 新 helpers(libFilter/bandShort/cardSaveOf/cardRoundOf/recommendFor/miniCardHtml/libRowHtml/contRowHtml)+ onShow/syncSubNav/goLibrary/goLearn/各 anchor/setLibFilter/renderHome/renderContinue/renderLibrary;loadLangCards 完成后按可见视图重绘
- 校验:html_js_check blocks 0-3 PASS;M6a 验收清单(手机:首页→测档→推荐卡→玩→返回语言首页)待小徐真机复测;本段为压缩会话补录

## 2026-09-05 语言文游 M7 打磨(worker Version e5458c87 + commit 6512ecb)
- ①渐进沉浸状态机(M7a):两处硬编码 round≤2 阈值(生成侧 buildInjection〔小译〕/辅助侧自动展开译卡)收敛为 LangEngine.immStage(round) 单点——scaffold(前 2 轮:可附〔小译〕+ 译卡自动展开)→ fade(第 3 轮起纯英语,辅助手动);full 全程无自动辅助;辅助侧经 window.LangEngine.immStage 共用;顺带修 renderPicker 默认档高亮 bug(profile.immersion 空时默认 progressive 不高亮)
- ②语气词典扩充(M7b):TONE_RULES 10→13 类(+cozy 温馨治愈/nautical 航海/contemporary urban 都市职场);英文卡 genre 含中文题材词时命中对应 register,与卡内 voice notes 同向(bible 仍是主源:命中兜底不改写设定)
- ③额度边界封口(M7c):考古结论=剧情续写(含语言卡)全走 /chat/completions 同一入口,免费/云币/会员边界无缺口;唯一开口=gloss/recap 直调模型仅 6s 内存限频,已登录零余额用户可刷付费兜底模型 → worker 加日配额 KV langaux:{kind}:{uid}:{date}(与 freequota 分桶):gloss 免费 60/会员 300,recap 免费 20/会员 60;成功才计数(与剧情免费额度同语义);超限 429 GLOSS_DAILY_LIMIT/RECAP_DAILY_LIMIT(明天 08:00 刷新);前端句下译卡改显专属提示;冒烟:gloss/recap 无 token 401 路由存活
- ④旧英语档处置(M7d,D8):考古确认入口已全部隔离(语言首页/剧本库/继续学习仅列 lang_cards;官方卡 id 与 lang_cards id 命名空间隔离,推荐/继续行不误拾);老官方卡英语存档保留,引擎 restoreIfNeeded 通道在=重启可续,中文大厅重进自动 exitToChinese;本处更新遗留旧文案(21887 提示改"英语原生剧本请在学语言→剧本库")+ 注释固化 D8 决策
- 待办:语言文游 v2 全链复测(小徐真机:五档卡各玩通+M6a 页面组走查+沉浸切换),README/部署清单收尾即本段
